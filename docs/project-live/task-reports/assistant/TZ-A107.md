# TZ-A107 — Real ERP data acceptance

DATE: `2026-07-17`
PHASE STATUS: `COMPLETE_OWNER_ACCEPTANCE_PASS`
BRANCH: `assistant-v1`
ASSISTANT BASE COMMIT: `a3f652313daa780909d65ee698d2d6f48d7abb2a`
CORE GATE COMMIT REVIEWED: `c765f59f2ac27ea9b6763a70c4e65a2be3d26c95`
CONTRACT VERSION: `0.4`
TEST BRANCH REF: `gsglkmudcwkdetqtocae`

## Core and safety gate

- The A107 Core gate sources were read from `origin/copilot-v1` with `git show` after `git fetch origin`; no merge or rebase was performed.
- The approved QA dataset and security gate are limited to test branch `gsglkmudcwkdetqtocae`.
- Runtime allowlist requires both Supabase URLs to parse as HTTPS and have hostname exactly equal to `gsglkmudcwkdetqtocae.supabase.co`; `A107_BRANCH_REF` must exactly equal the branch ref.
- The guard runs before auth, OpenAI, and ERP access and fails closed for a missing/malformed URL, any different hostname/ref, or any loaded service-role, database, direct, or admin credential.
- Guard regression: `6/6 PASS`.
- Production connections: `0`; ERP writes: `0`; service role loaded: `NO`.

## Clean build and bundle scan

- `.env` and `.env.local` were not modified or copied into the build/runtime contexts.
- The safe build used a clean process environment with only the test-branch public URL/key and required non-administrative A107 variables.
- OpenAI key loading happened separately only after the bundle scan; the existing ignored key was reused without printing or changing it.
- Build: `PASS`.
- Production project-ref file matches in `.next`: `0`.
- Production Supabase URL file matches in `.next`: `0`.
- Allowed branch-ref file matches in `.next`: `44`.
- Allowed branch-host file matches in `.next`: `44`.
- Service-role secret matches in `.next`: `0` because no service-role secret was loaded into build or runtime.
- The recursive scan covered all `.next` server/client artifacts; this build emitted `0` source-map files.

## Auth preflight

- QA User A obtained a valid authenticated session on the test branch.
- Destination hostname: `gsglkmudcwkdetqtocae.supabase.co`.
- Runtime guard before auth: `PASS`.
- Production connections: `0`.
- ERP writes: `0`.
- Service role loaded: `NO`.

## Real acceptance

- Requested/effective model: `gpt-5.6-terra`.
- Reasoning effort: `medium`.
- Responses API: `store:false`.
- Model tools: exactly eight approved read-only ERP tools.
- Scenarios: `45/45 PASS`.
- ERP numeric accuracy: `100.00%`.
- Field search: `PASS`, including partial name, exact area, multiple matches, no match, and ambiguity clarification.
- Follow-up state: `PASS`, including field, crop, area, operations, and materials continuity.
- Unit accuracy: `100%`; kilograms and litres remained distinct.
- Cross-company leaks: `0`; Company B data exposed: `NO`.
- ERP mutations: `0`.
- Production connections: `0`.

Verified ground truth includes 8 fields, 1,000 ha total area, 5 operations (2 active, 2 completed, 1 planned), 1,550 kg ammonium nitrate, 520 l Curamin Foliar, and 200 l Phomazin. All numeric answers matched the QA dataset exactly.

## Regression and evidence

- `npm run typecheck`: `PASS`.
- `npm run qa:assistant:readonly-v1`: `24/24 PASS`.
- `git diff --check`: `PASS` (line-ending warnings only).
- Required local evidence exists outside Git under `audit-output/TZ-A107/`: nine requested artifacts plus `acceptance-summary.json`.
- No secret is stored in the audit outputs.

## Owner gate

OWNER TEST URL: `http://127.0.0.1:3106/dashboard`
OPENAI MODE: `REAL`
MODEL: `gpt-5.6-terra`
REASONING EFFORT: `medium`
TEST BRANCH REF: `gsglkmudcwkdetqtocae`
PRODUCTION CONNECTIONS: `0`
ERP WRITES: `0`
LOCAL SERVER STATUS: `RUNNING`
READY FOR OWNER ACCEPTANCE: `YES`
OWNER ACCEPTANCE: `PASS`

The owner returned `OWNER_ACCEPTANCE: PASS` after the final REAL regression. Selective A107 commit and push to `origin/assistant-v1` are authorized. Merge, production deploy, and production access remain forbidden.

## Owner finding correction — Copilot context connection

The owner reported that the dashboard still displayed the correct QA totals while Travkin Copilot showed a context-load error and `Failed to fetch`. The frontend was already using the correct same-origin relative endpoint, `/api/assistant/context`; therefore there was no configured cross-origin URL, port mismatch, or `localhost` versus `127.0.0.1` mismatch. The immediate network failure occurred because the foreground owner runtime had exited, while the already loaded browser page could continue reading QA data directly from Supabase.

A second defect existed behind the connection failure: the context route authenticated the user with a request-scoped JWT but then instantiated the forbidden service-role client for company/season reads. The route now runs the exact A107 branch guard before auth and uses the same request-scoped JWT/anon client under RLS for company and season reads. No service-role key is loaded.

The owner launcher now supports a hidden detached process, redirects only non-secret runtime output to ignored local audit logs, and verifies that port 3106 is listening before returning. A clean dotenv-free rebuild passed with production matches `0/0`, test-branch matches `44/44`, service role `0`, and database/admin credentials `0`.

Real auth/context preflight: `PASS`. Destination hostname was exactly `gsglkmudcwkdetqtocae.supabase.co`; the context returned QA company `8a0f2c0e-6638-4a31-99a8-cab4237d287d`. Browser reload at `http://127.0.0.1:3106/dashboard` showed 8 fields, 1,000 ha, Copilot context `Астык-STEM QA · 2026 · agronomist`, an enabled input, and no new frontend/network errors. A real read-only question returned: `В компании 8 полей общей площадью 1000 га`.

OWNER FINDING STATUS: `FIXED_ACCEPTED`
COPILOT API URL: `http://127.0.0.1:3106/api/assistant/context`
CONTEXT LOAD: `PASS`
INPUT: `PASS`
READ-ONLY ANSWER: `PASS`
PRODUCTION CONNECTIONS: `0`
ERP WRITES: `0`
SERVICE ROLE LOADED: `NO`
LOCAL SERVER STATUS: `RUNNING_DETACHED`
OWNER ACCEPTANCE: `PASS`

## Owner finding correction — warehouses, product names, generic field list

The owner reported three grounded read-path failures. They were traced to separate causes:

1. `get_warehouse_stock` exposed an optional product parameter, but its implementation always read stock rows and the planner had no deterministic warehouse-directory route. The same read-only layer already contained a JWT/RLS query for canonical active warehouses. That query is now an internal directory mode of the existing model-facing tool, so the model-facing surface remains exactly eight tools. A grounded post-processor always includes both returned warehouse names for a warehouse count/list request.
2. Product filtering only compared the displayed canonical name. It now compares canonical, `name_ru`, `name_kz`, `name_en`, and trade name, supports unambiguous partial matching, and uses generic Cyrillic-to-Latin transliteration with one-character tolerance. Short product-like verbatim phrases are forced through `get_warehouse_stock`; greetings, questions, fields, warehouses, operations, and other conversational phrases are excluded. Multiple matched canonical products produce a clarification request.
3. `Какие поля есть?` was parsed as a field named `есть`, and an existing selected-field focus could also narrow the generic list. Generic list routing now clears both the accidental parsed name and selected-field focus, then merges the active-season crop structure into all eight concise field rows.

Real QA branch evidence confirms two warehouses (`Основной склад`, `Полевой склад`), eight fields, and Curamin Foliar stock `520 l` (`480 l` + `40 l`). The product row has `name`, `trade_name`, `name_en`, and `name_ru` all equal to `Curamin Foliar`; therefore the requested Russian Core dataset alias is absent. No Core row was changed. The exact branch-only Core proposal is recorded in `docs/project-live/contract-proposals/assistant/2026-07-17-a107-curamin-localized-alias.md`.

The focused REAL regression passed all seven owner scenarios with exact quantities and an unchanged ERP snapshot before the final deterministic warehouse-answer hardening. Browser retest then passed verbatim `Курамин` and the eight-field list in the existing focused chat. The final clean build passed with production matches `0/0`, branch matches `44/44`, service role `0`, and DB/admin credentials `0`.

After OpenAI quota restoration, the final uninterrupted REAL repetition passed `7/7`: the two-warehouse directory, all five Curamin Foliar name forms, and the concise eight-field list matched the isolated QA dataset. A focused follow-up/security regression also passed `6/6`, including selected-field continuity, Company B denial, and write-command denial. The ERP snapshot remained unchanged. No mock or model fallback was used. The owner accepted the result.

WAREHOUSE DIRECTORY: `PASS`
PRODUCT RESOLUTION LOGIC: `PASS`
CURAMIN RU DATASET ALIAS: `MISSING_CORE_PROPOSAL_CREATED`
GENERIC FIELD LIST: `PASS`
FOCUSED REGRESSION: `7/7 PASS`
FINAL REPETITION: `7/7 PASS_AFTER_QUOTA_RESTORATION`
PRODUCTION CONNECTIONS: `0`
ERP WRITES: `0`
SERVICE ROLE LOADED: `NO`
OWNER ACCEPTANCE: `PASS`

## Final owner acceptance and deferred UX backlog

The owner returned `OWNER_ACCEPTANCE: PASS` and explicitly accepted one non-blocking UX issue: the conversational praise `Маладээс` was misclassified as a product lookup. Future intent hardening must distinguish praise and colloquial phrases from catalog requests, tolerate variants such as `молодец`, `маладец`, and `маладээс`, and never start an ERP search without an explicit subject request.

The issue is recorded as `INTENT-UX-001` in `docs/project-live/backlog/assistant-intent-ux-hardening.md`. It does not reopen or block A107.

A107 FINAL STATUS: `COMPLETE_OWNER_ACCEPTANCE_PASS`
KNOWN MINOR UX ISSUE: `RECORDED_NON_BLOCKING`
READY FOR PREVIEW INTEGRATION: `YES_AFTER_SELECTIVE_PUSH`
