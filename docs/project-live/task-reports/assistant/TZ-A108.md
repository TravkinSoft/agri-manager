# TZ-A108 — Preview integration package

DATE: `2026-07-18`
PHASE STATUS: `COMPLETE_OWNER_APPROVAL_PASS`
SOURCE BRANCH: `assistant-v1`
SOURCE COMMIT: `a71e292e85a1c7d7b7503275174c34f94fa39bd5`
CORE COMMIT REVIEWED: `abce1bb9e18fc118c68dfc6add6fb31d05ffe81c`
MERGE BASE: `d19258762bb7eaf2afcca94eb7d611d56eedbd41`
TEST BRANCH REF: `gsglkmudcwkdetqtocae`

## Scope and inventory

`git fetch origin` completed before comparison. No merge or rebase was performed. The source commit equals `origin/assistant-v1`, and the reviewed Core commit equals `origin/copilot-v1`.

The Assistant delta from the shared merge base contains 58 files: 43 added and 15 modified. The selective temporary preview applies 56 files. The two excluded files are Core-owned `docs/project-live/ASSISTANT_LIVE_STATE.md` and `docs/project-live/ASSISTANT_SYNC_STATE.md`; their Assistant versions remain handoff documentation rather than files to overwrite in Core.

Both branches changed exactly three paths:

1. `package.json` — integrated manually as a union that preserves Core scripts `qa:warehouse:units-v2`, `qa:glbd:component-discovery`, and `@electric-sql/pglite`, then adds only Assistant QA scripts.
2. `docs/project-live/ASSISTANT_LIVE_STATE.md` — preserve Core target; use this report for handoff.
3. `docs/project-live/ASSISTANT_SYNC_STATE.md` — preserve Core target; use this report for handoff.

The package contains five Assistant API routes, the Copilot UI panel changes, server conversation and Memory Policy V2 runtime, request-scoped Supabase user client, eight read-only ERP tools, runtime guards, and QA harnesses. There are no Assistant migrations to transfer. Core already contains `supabase/migrations/20260716125205_assistant_memory_policy_v2.sql`; the validated data target remains only the test branch.

The exact per-file source, purpose, unchanged-transfer decision, conflict, integration method, and rollback are recorded in `audit-output/TZ-A108/integration-manifest.json`.

## Detached integration copy

Temporary worktree: `C:\Users\TRAVKIN\Downloads\CodecSaaS\project-assistant-a108-preview-temp`.

It was created detached at the exact Core commit. The real `copilot-v1` worktree and branch were not changed. Only manifest-selected Assistant files and the documented package union were applied.

The first complete REAL A107 run passed 44/45 and exposed a semantic integration conflict in `lib/assistant/v1/engine.ts`: the short product lookup heuristic interpreted the inflected business phrase `поливы` as a warehouse product request. A108 resolves it with `audit-output/TZ-A108/resolutions/lib-assistant-v1-engine.patch`, adding praise variants and suffix-aware irrigation/business-subject exclusions. The focused REAL scenario then passed 1/1, and the rebuilt full suite passed 45/45.

Conflicts found: `4` (three overlapping paths plus one acceptance-discovered semantic conflict). Conflicts resolved: `4`.

## Safety and build

The clean build process did not load dotenv files or `OPENAI_API_KEY`. It set only the exact test-branch public URL/key and non-administrative runtime variables. OpenAI key loading occurred separately after the bundle scan and used the existing ignored local value without printing or changing it.

- Build: `PASS`.
- Typecheck: `PASS`.
- Production/disallowed Supabase ref matches: `0`.
- Production/disallowed Supabase URL matches: `0`.
- Exact test-branch ref files: `46`.
- Exact test-branch hostname files: `46`.
- Source map files: `0`.
- Service-role credential loaded or bundled: `NO` / `0`.
- Database/direct/admin credentials loaded: `NO`.

Server chunks contain only defensive env variable names used by fail-closed Core/Assistant guards; no credential value was loaded into the build or runtime.

## Regression results

- A106 Memory Policy V2 REAL regression: `10/10 PASS`.
- A107 REAL ERP regression: `45/45 PASS`.
- ERP numeric accuracy: `100.00%`.
- Greeting regression: `4/4 PASS`.
- Owner warehouses/product/field regression: `7/7 PASS`.
- Warehouses: `2`, with both expected names.
- Curamin/Курамин/Фолиар resolution: `PASS`, total `520 l`, split `480 l + 40 l`.
- Generic field list: `8/8`, with area and crop data.
- Follow-up: `PASS`.
- Unit accuracy: `PASS`.
- Cross-company leaks: `0`.
- ERP writes/mutations: `0`.
- Production connections: `0`.
- Responses API storage: `false`.
- Requested/effective model: `gpt-5.6-terra`.
- Reasoning effort: `medium`.

Transport smoke returned HTTP 200 for `/`, `/auth/login`, `/dashboard`, `/fields`, `/warehouses`, `/operations`, `/weighbridge`, and `/references`. Owner validation proved that this was not a valid Core smoke result because several shells rendered error states instead of real data. `CORE_SMOKE` is therefore corrected to `FAIL_DATA_LEVEL`.

## Preview environment handoff

The value-free checklist is `audit-output/TZ-A108/preview-env-checklist.md` and is duplicated structurally in the JSON manifest. It requires the exact test-branch public URL/anon variables, branch refs, strict runtime gates, `gpt-5.6-terra`, medium reasoning, Responses `store:false`, Memory V2 gates, and a runtime-only OpenAI key. Service-role/admin keys, database/direct credentials, and every non-test Supabase project are forbidden.

## Final gate

A108 STATUS: `COMPLETE_OWNER_APPROVAL_PASS`
READY FOR CORE PREVIEW INTEGRATION: `YES`
BLOCKERS: `NONE`
NEXT ACTION: `Publish the selective A108 package to origin/assistant-v1. Core preview integration may proceed separately; merge and deploy remain forbidden.`

No A108 commit, push, merge, deploy, production access, database write, ERP write, or `copilot-v1` mutation was performed.

## Owner preview failure — Core data pages

The owner returned `OWNER_APPROVAL: FAIL`. Browser reproduction in the authenticated QA User A session confirmed:

- `/crop-structure`: page shell is 200, but the content displays `Не удалось загрузить структуру посевов` and `Supabase service credentials are not configured`; no structure data is shown.
- `/warehouses`: page shell is 200, but the page and destructive toast display the same credentials error; the UI falls back to 0 warehouses and empty stock.
- `/operations`: the main journal eventually displays five operations through direct user-RLS reads, but opening an operation calls `GET /api/operations/[id]/lines`, whose Core route uses `getServiceClient()` and surfaces the credentials error.
- `/warehouses`: the observer badge visibly renders mojibake instead of Russian text.

The root is Core-owned. A targeted diff against Core commit `1fbb3998c8dc82ee8e4af0b439b8a32c0b76a034` is empty for the three pages, their service modules, `lib/auth/server-session.ts`, `lib/supabase/service.ts`, and the affected API route trees. A108 correctly omits the service role; adding it would violate the safety contract.

The exact Core proposal is `docs/project-live/contract-proposals/assistant/2026-07-17-a108-core-user-jwt-data-pages.md`. It requires request-scoped user-JWT clients, RLS/company predicates, no privileged fallback, explicit grants/RLS migration only if needed, UTF-8 literal corrections, and a real data-level Core smoke harness.

The corrected acceptance rule is: HTTP 200 proves only that a shell rendered. Core smoke passes only when authenticated pages finish loading expected QA data, API status/schema assertions pass, no destructive error banner/toast or permanent loading state remains, mojibake is absent, cross-company leaks are 0, ERP writes are 0, service role is absent, and production connections are 0.

## Core resync at 5b5a57c and current blocker

On 2026-07-18, `git fetch origin` resolved `origin/copilot-v1` to `5b5a57c8b8490340c92b72e2a74a4ca4404d4613` (`fix(preview): load core data with user JWT`). The old temporary worktree and its confirmed port-3106 process were removed, then a fresh detached worktree was created at that exact commit. The verified manifest selected exactly 56/56 files, with no extra paths; the two Core-owned state documents and all new Core data routes remained unchanged. The recorded semantic patch applied cleanly.

Fresh validation results:

- Typecheck: `PASS`.
- Clean production build: `PASS`.
- Bundle production ref/URL matches: `0/0`; exact branch ref/URL files: `46/45`.
- Runtime auth/context preflight as QA User A: `PASS`; destination host `gsglkmudcwkdetqtocae.supabase.co`.
- `/crop-structure`: `PASS`, 9 crop-structure rows rendered (8 fields; Field 31 has two crops), no error banner/toast, no permanent loading.
- `/operations`: `PASS`, 5 operations rendered; the Field 28 `spraying` side sheet opened with crop, variety, reproduction, area, status, and comment; authenticated `GET /api/operations/[id]/lines` returned 200 and an array without a service-credentials error.
- Cross-company probes: `PASS`; User A receives 403 from company-B Core endpoints and reads zero company-B operation rows.
- Mojibake in the three smoke pages: `0`.
- A106 Memory Policy V2: `10/10 PASS`.
- A106 real greeting regression: `4/4 PASS`.
- A107 REAL ERP: `45/45 PASS`, numeric accuracy `100.00%`, cross-company leaks `0`, ERP mutations `0`, production connections `0`.

The warehouse smoke still fails. `/warehouses` displays a destructive banner/toast, 0 warehouses, and the error `Could not find a relationship between 'stock_ledger_entries' and 'variety_id' in the schema cache`. The Core harness `scripts/qa/smoke-core-preview-data.ts` independently reproduces HTTP 400 at `/api/warehouses/balances` before its warehouse assertions.

The root is Core-owned `app/api/warehouses/balances/route.ts`, which embeds `varieties:variety_id` and `reproductions:reproduction_id`. Migration `supabase/migrations/20260510093000_add_batch_class_and_identity_flow.sql` explicitly drops `stock_ledger_entries_variety_id_fkey` and `stock_ledger_entries_reproduction_id_fkey`, so PostgREST cannot infer either relationship. None of these files is transferred by the A108 manifest. No Core code or database schema was changed in Assistant.

At that checkpoint, the REAL preview remained running at `http://127.0.0.1:3106/operations` for owner inspection. A108 was not ready for owner approval until the warehouse page rendered 2 warehouses and the expected `1550 kg / 520 l / 200 l` balances without an error state. No commit, push, merge, deploy, production connection, service-role load, or ERP write was performed.

## Final Core resync at abce1bb

On 2026-07-18, `git fetch origin` resolved `origin/copilot-v1` to `abce1bb9e18fc118c68dfc6add6fb31d05ffe81c` (`fix(warehouses): load balances without legacy embeds`). The preceding preview process/worktree was removed only after exact path and port-owner verification. A fresh detached copy was created at that Core commit, then the A108 manifest applied exactly 56/56 selected files plus the recorded semantic patch. Core's new `app/api/warehouses/balances/route.ts` remained unchanged by Assistant.

Fresh final validation:

- Typecheck and clean production build: `PASS`.
- Bundle production ref/URL matches: `0/0`; exact test-branch ref/URL matches: `46/45` files.
- Auth/context preflight under QA User A: `PASS`; exact destination `gsglkmudcwkdetqtocae.supabase.co`.
- Core data smoke: `PASS` — 9 crop rows, 2 warehouses, 1550 kg ammonium nitrate, 520 l Curamin Foliar, 200 l Phomazin, 5 operations, operation details, company-B denial, mojibake 0, error payloads 0.
- Authenticated UI smoke: `PASS` — crop structure rendered, both warehouse cards and detailed stock dialogs rendered without error banner/toast, and the Field 28 spraying card opened with complete details.
- A106 Memory Policy V2: `10/10 PASS`; real greeting regression: `4/4 PASS`.
- A107 REAL ERP: `45/45 PASS`; numeric accuracy `100.00%`; cross-company leaks `0`; ERP mutations `0`; production connections `0`; service role loaded `NO`; Responses store `false`.

The previous warehouse relationship blocker is resolved and no A108 blocker remains. The REAL preview stays running at `http://127.0.0.1:3106/operations` for owner validation. No commit, push, merge, deploy, production connection, service-role load, database credential, or ERP write was performed.

## Final owner approval

The owner returned `OWNER_APPROVAL: PASS` on 2026-07-18. A108 is complete and its verified selective package is authorized for commit and push to `origin/assistant-v1`. The package is ready for Core preview integration; merge into `copilot-v1` and every deploy remain explicitly forbidden.
