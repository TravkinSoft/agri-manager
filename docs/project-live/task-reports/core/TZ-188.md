# TZ-188 - Apply pesticide search aliases Batch 1

DATE: `2026-07-18`
STATUS: `DONE`
CORE_BRANCH: `copilot-v1`
START_COMMIT: `7779b7578f272adb46932488c6f527da2acdb615`
OWNER_APPROVAL: `APPROVED`
PRODUCTION_PROJECT: `bhsemlvmkikpntabctml`

## Backup and preflight

A fresh exact-scope backup was created before production writes:

`C:/Users/TRAVKIN/Downloads/CodecSaaS/audit-output/TZ-188/backups/pesticide-alias-batch-one-20260718T073308550Z`

- backup manifest SHA-256:
  `f31d388922efdf413c94689fdcd35dc72af3f36c4890d0a27cb3910e16683649`;
- TZ-187 manifest SHA-256:
  `7486e4bf0d272a4e3ea84673367a996e81f8b811500d7f9acb1be588f85579cf`;
- cards found: `50/50`;
- exact aliases before apply: `0`;
- same-product conflicts: `0`;
- foreign identity/alias collisions: `0`;
- company targets: `0`;
- aliases before: `100`;
- production health before apply: `ACTIVE_HEALTHY`.

The backup includes the exact approved actions, all 50 product rows, their
existing aliases, the complete alias baseline, preflight evidence, guarded
apply SQL, exact rollback SQL and verified manifest. No secrets are stored.

## Production apply

One explicit transaction inserted only the approved 50 rows into
`global_product_aliases`. The SQL repeated the product, trade-name, tenant,
collision and partial-state guards inside the transaction.

- cards processed: `50`;
- aliases inserted: `50`;
- aliases total: `100 -> 150`;
- card updates: `0`;
- trade-name/manufacturer/formulation/component/usage updates: `0`;
- company-data changes: `0`.

The mandatory second execution returned `inserted=0`. No migration, db push,
deploy or master merge was performed.

## Post-apply verification

- exact approved aliases present: `50/50`;
- duplicate alias groups: `0`;
- foreign alias conflicts: `0`;
- 50 short-query searches: `50/50 PASS`;
- ambiguous results: `0`;
- Curamin/Курамин/Фолиар and Phomazin/Фомазин controls: `5/5 PASS`;
- all 50 product rows match the backup fingerprint;
- all company product rows match the backup fingerprint;
- company alias targets: `0`;
- critical route smoke: `6/6 HTTP 200`;
- production health after apply: `ACTIVE_HEALTHY`;
- rollback required: `NO`.

The first control verifier incorrectly accepted only the EN control identity,
although the RU queries correctly returned the active global fertilizer cards
`Курамин Фолиар` and `Фомазин`. The verifier was corrected to the already
approved RU/EN identity groups and the full post-check then passed. This was a
test expectation bug, not a production search failure.

## Decision

`READY_FOR_ALIAS_BATCH_2=YES_WITH_OWNER_APPROVAL`. Batch 2 remains a separate
production change and requires a new numbered task, explicit owner approval,
fresh backup and live no-drift preflight.
