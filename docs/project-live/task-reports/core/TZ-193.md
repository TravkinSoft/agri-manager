# TZ-193 - Apply the next 200 pesticide search aliases

DATE: `2026-07-18`
STATUS: `DONE`
CORE_BRANCH: `copilot-v1`
START_COMMIT: `9c34cf78075123932cfc747f2e458b8495444cf7`
OWNER_APPROVAL: `APPROVED`
PRODUCTION_PROJECT: `bhsemlvmkikpntabctml`

## Package and backup

The package uses the next deterministic TZ-187 actions after TZ-188 and
TZ-191: the remaining `48` actions from Batch 06, all `150` actions from
Batches 07-09 and the first `2` actions from Batch 10.

The two rejected `Идеал` aliases, the owner HOLD cards `Дитан`, `Метамил` and
`Курзат`, all `15` TZ-181 unresolved cards and company products are excluded.
The selected package has exactly `200` products, alias IDs and normalized
aliases.

Fresh exact-scope backup:

`C:/Users/TRAVKIN/Downloads/CodecSaaS/audit-output/TZ-193/backups/pesticide-alias-batch-three-20260718T113829396Z`

- backup manifest SHA-256:
  `98ec5419dc6a0c6765660437f7d4927d7cb1d14a7257a95589ce98a08b8b3de6`;
- approved package manifest SHA-256:
  `a5cc7117440cc55d22cb14149bae7be0b937d1c7f69d3564eae5629b490d1ac5`;
- TZ-187 source manifest SHA-256:
  `7486e4bf0d272a4e3ea84673367a996e81f8b811500d7f9acb1be588f85579cf`;
- cards and aliases: `200/200`;
- existing exact aliases, duplicate keys, conflicts and exact ambiguities:
  `0/0/0/0`;
- company targets: `0`;
- aliases before: `350`;
- production health: `ACTIVE_HEALTHY`.

All backup files match the manifest. The apply file contains one guarded
`INSERT` statement inside one transaction and no product `UPDATE` or `DELETE`.

## Production apply and postcheck

The owner-approved transaction inserted exactly `200` rows into
`global_product_aliases`.

- aliases total: `350 -> 550`;
- exact search: `200/200 PASS`;
- exact search ambiguities: `0`;
- duplicate alias groups: `0`;
- alias conflicts: `0`;
- Curamin/Курамин/Фолиар and Phomazin/Фомазин controls: `5/5 PASS`;
- target product fingerprint: unchanged;
- company product fingerprint: unchanged;
- mandatory second apply: `inserted=0`;
- final production health: `ACTIVE_HEALTHY`;
- rollback required: `NO`.

The `27` broader substring family matches are recorded separately and do not
share an exact normalized alias. Every exact alias has exactly one owner.

Trade names, manufacturers, composition, formulation, registration, usage
rules and company data were not changed. No migration, db push, deploy or
master merge was performed.

## Decision

`READY_FOR_NEXT_ALIAS_BATCH=YES_WITH_OWNER_APPROVAL`. The TZ-187 package has
`112` remaining owner-eligible aliases. A later task must repeat the fresh
backup and live no-drift preflight.
