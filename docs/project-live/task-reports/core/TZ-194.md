# TZ-194 - Apply the final 112 pesticide search aliases

DATE: `2026-07-18`
STATUS: `DONE`
CORE_BRANCH: `copilot-v1`
START_COMMIT: `d626ef36c96dfc2b10f7fd3ccaaae22b192c616b`
OWNER_APPROVAL: `APPROVED`
PRODUCTION_PROJECT: `bhsemlvmkikpntabctml`

## Final package and backup

The package is the exact final `112`-action tail of the verified TZ-187 batch
plan after the `200` aliases applied by TZ-193. All `200` TZ-193 rows were
reconfirmed in production before this package was accepted.

The two rejected `Идеал` aliases, the owner HOLD cards `Дитан`, `Метамил` and
`Курзат`, all `15` TZ-181 unresolved cards and company products are excluded.
The final package has exactly `112` products, alias IDs and normalized aliases.

Fresh exact-scope backup:

`C:/Users/TRAVKIN/Downloads/CodecSaaS/audit-output/TZ-194/backups/pesticide-alias-batch-four-20260718T184555786Z`

- backup manifest SHA-256:
  `283fe1f4930e4a089d61db44197b163ebd386f2f1145f3e2183afbb67831e56d`;
- approved package manifest SHA-256:
  `0defaa8e250cd46ed61751140016d4db78ed6b82da09cce40ec4e3085bcf2997`;
- TZ-187 source manifest SHA-256:
  `7486e4bf0d272a4e3ea84673367a996e81f8b811500d7f9acb1be588f85579cf`;
- cards and aliases: `112/112`;
- existing exact aliases, duplicate keys, conflicts and exact ambiguities:
  `0/0/0/0`;
- previous TZ-193 rows verified: `200/200`;
- company targets: `0`;
- aliases before: `550`;
- production health: `ACTIVE_HEALTHY`.

Every backup file matches the manifest. The apply file contains one guarded
`INSERT` statement in one transaction and no `UPDATE` or `DELETE`.

## Production apply and acceptance

The owner-approved transaction inserted exactly `112` rows into
`global_product_aliases`.

- aliases total: `550 -> 662`;
- exact search: `112/112 PASS`;
- exact search ambiguities: `0`;
- duplicate alias groups: `0`;
- alias conflicts: `0`;
- Curamin/Курамин/Фолиар and Phomazin/Фомазин controls: `5/5 PASS`;
- target product fingerprint: unchanged;
- company product fingerprint: unchanged;
- mandatory second apply: `inserted=0`;
- final production health: `ACTIVE_HEALTHY`;
- rollback required: `NO`.

The `7` broader substring family matches do not share an exact normalized
alias. Every exact alias has exactly one owner.

Trade names, manufacturers, composition, formulation, registration, usage
rules and company data were not changed. No migration, db push, deploy or
master merge was performed.

## Decision

`TZ187_SAFE_ALIAS_PACKAGE_COMPLETE=YES`. Remaining owner-eligible aliases from
the TZ-187 package: `0`. Further catalog work requires a new source-backed
audit package and a separately approved numbered task.
