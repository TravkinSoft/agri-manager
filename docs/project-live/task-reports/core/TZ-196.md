# TZ-196 - Final pesticide GLBD V1 readiness audit

DATE: `2026-07-19`
STATUS: `DONE`
CORE_BRANCH: `copilot-v1`
PRODUCTION_PROJECT: `bhsemlvmkikpntabctml`
MODE: `READ_ONLY_PRODUCTION_AUDIT_AND_APPLY_PREVIEW`

## Scope and safety

The scope is exactly the `15` unresolved TZ-181 cards plus `Дитан`, `Метамил`
and `Курзат`: `18` rows and `18` unique product IDs. All live reads were
limited to global pesticide catalog data. Production writes, product merges,
migrations, deploys and company-data reads/writes are `0`.

Critical facts were accepted only from a manufacturer, official label,
government registry/publication or official distributor. Imported free-text
composition guesses were treated as untrusted. Critical preview actions with
a source are `9/9`; guessed compositions and formulations are `0/0`.

## Remaining 18 decisions

| Classification | Cards |
| --- | ---: |
| SAFE_COMPLETE | 1 |
| SAFE_PARTIAL | 2 |
| SAFE_ADD_ALIAS | 0 |
| SAFE_IDENTITY_NORMALIZE | 0 |
| KEEP_SEPARATE | 3 |
| KEEP_INACTIVE | 2 |
| BLOCKED_NO_DATA | 10 |
| NEED_OWNER_DECISION | 0 |

`Lastik Extra` is the only complete card: Avgust confirms the herbicide
identity, fenoxaprop-P-ethyl `70 g/L`, cloquintocet-mexyl safener `40 g/L`
and EC formulation. `Праймур, СЭ` has official Kazakhstan composition and
formulation evidence but remains partial because the manufacturer is not
confirmed. `Amino Max` is also partial because the official fertilizer record
does not prove the exact manufacturer/variant of the live row.

`Amino Alexin` and `ES Biograno Forte` are source-backed fertilizer or
biostimulant products and are marked `KEEP_INACTIVE` for the pesticide
catalog. No production visibility change was applied.

`Дитан`, `Метамил` and `Курзат` remain separate. Reliable sources confirm
`Dithane M-45`, `Метамил МЦ` and `Курзат Р`, but the incomplete short-name
rows do not prove that they are those exact commercial products. No identity,
composition or link was transferred.

## Assistant safety matrix

All `852` global pesticide rows were audited, including inactive and archived
rows. The result is:

- `ASSISTANT_READ_READY = 19`;
- `ASSISTANT_READ_PARTIAL = 815`;
- `ASSISTANT_BLOCKED = 18`;
- `ASSISTANT_RECOMMENDATION_READY = 0`;
- `ASSISTANT_RECOMMENDATION_BLOCKED = 852`.

The read matrix can gate A110. A110 may start only as read-only and must
exclude `READ=NO` rows plus the `2` source-reviewed rows whose corrected facts
exist only in the unapplied preview. Registration and usage rules are not yet
complete enough for any agronomic recommendation.

## Apply preview

The preview contains four exact-ID cards only:

1. Correct Lastik Extra category/formulation and add the active+safener links.
2. Correct Праймур formulation and add its two active links.
3. Make Amino Alexin inactive in the pesticide catalog.
4. Make ES Biograno Forte inactive in the pesticide catalog.

Every action requires a fresh backup, live preflight and separate owner
approval. Duplicate component actions, alias conflicts and product merges are
`0/0/0`.

## Acceptance

- products reviewed: `18/18`;
- unique IDs: `18/18`;
- unclassified: `0`;
- global pesticide matrix: `852/852`;
- critical facts with source: `100%`;
- production writes: `0`;
- first/second audit fingerprint match: `PASS`;
- audit fingerprint: `dd5ea71585ac35472eb74db89b7fb831717da30faa99f133affd0c894402f797`.

The reproducible script is
`scripts/audit/finalize-pesticide-glbd-v1-readiness.mjs`. External evidence is
stored at `C:/Users/TRAVKIN/Downloads/CodecSaaS/audit-output/TZ-196/` and is
not committed.
