# TZ-180 - Global pesticide cards completeness audit V1

DATE: `2026-07-17`
STATUS: `DONE`
CORE_BRANCH: `copilot-v1`
PRODUCTION_PROJECT: `bhsemlvmkikpntabctml`
MODE: `READ_ONLY_PRODUCTION_AUDIT`

## Scope and preflight

Production was read only and remained `ACTIVE_HEALTHY`. The verified GLBD
baseline was `431` components, `63` component aliases, `318` component sources,
`1373` product-component links and `0` company links. Active component
duplicates and mojibake findings were both `0`; staged files were `0`.

The audited scope was derived live rather than assumed:

`products.company_id IS NULL AND product_type = 'pesticide' AND archived = false`

This produced exactly `852` global pesticide cards. Fertilizers, additives,
seeds, machinery, company-local products and archived technical rows were
excluded. Only six minimal company-product identity rows were read to detect a
possible company/global conflict; no company business data was loaded.

## Readiness result

All `852` cards were classified exactly once. Product IDs are unique, duplicate
audit rows and unclassified rows are `0`.

| Readiness | Cards |
| --- | ---: |
| READY_COMPLETE | 0 |
| READY_WITH_MINOR_GAPS | 776 |
| BLOCKED_COMPONENT | 12 |
| BLOCKED_IDENTITY | 2 |
| BLOCKED_FORMULATION | 0 |
| BLOCKED_SOURCE | 38 |
| NEED_OWNER_REVIEW | 24 |

The critical blocked/review population is `76` cards. The `16` missing-form
cards also have higher-priority component/owner-review blockers, so none forms
a separate `BLOCKED_FORMULATION` or Batch 2 population.

## Findings

| Issue flag | Cards |
| --- | ---: |
| MISSING_COMPONENT | 34 |
| COMPONENT_SOURCE_GAP | 818 |
| COMPONENT_ROLE_CONFLICT | 0 |
| SAFENER_MODEL_GAP | 0 |
| BIOLOGICAL_MODEL_GAP | 0 |
| MISSING_FORMULATION | 16 |
| FORMULATION_CONFLICT | 0 |
| MISSING_MANUFACTURER | 799 |
| MANUFACTURER_IN_TRADE_NAME | 0 |
| MISSING_RU_ALIAS | 31 |
| MISSING_EN_ALIAS | 789 |
| IDENTITY_DUPLICATE | 2 |
| COMPANY_GLOBAL_CONFLICT | 0 |
| SOURCE_GAP | 850 |
| REGULATORY_GAP | 852 |
| USAGE_RULE_GAP | 821 |
| SEARCH_FAILURE | 24 |
| MOJIBAKE | 0 |
| NEED_OWNER_REVIEW | 24 |

`COMPONENT_SOURCE_GAP` means an existing product-component relation does not
carry its own source reference. `SOURCE_GAP` also records missing or invalid
card source metadata and missing metadata confidence. No existing component
links were changed.

The two identity duplicates are `Celest Top` and `Селест Топ, КС`; this audit
does not merge them. No manufacturer-prefix case or company/global conflict was
confirmed in the pesticide scope.

## Search acceptance

The audit used the real `catalog-identity` and component-discovery helpers. All
`852` cards received an official trade-name test; a deterministic `126`-card
matrix additionally covered RU/EN names, database aliases, shortened names,
manufacturer-prefixed legacy identities and multi-component search.

- total scenarios: `1059`;
- passed: `1009`;
- failed: `50` across `24` unique pesticide cards;
- failed scenario types: `24` RU-name and `26` database-alias tests.

The known Curamin/Phomazin tests passed in RU and EN. Their current global rows
are fertilizer-scope records and were correctly excluded from pesticide
readiness counts. The failures show that the current catalog search path does
not consistently consume stored RU names and `global_product_aliases`.

## Reproducibility and outputs

All 18 required artifacts were generated outside Git at:

`C:/Users/TRAVKIN/Downloads/CodecSaaS/audit-output/TZ-180/`

The manifest verifies with zero SHA-256 mismatches. A second complete run
returned the same audit fingerprint:

`2d74a26055cbad5a1466b591dc77ffc2926364ccafa06cf2d26f44f05401f696`

Production writes, product changes, component-link changes, migrations, deploy
and merge were all `0`.

## Batch decision

- Batch 1 / P0: `45` cards. Missing components, identity duplicates and current
  search failures. Source research and owner review are mandatory; automatic
  apply is not allowed.
- Batch 2 / P1: `0` independently assigned cards. Its `16` formulation gaps are
  already contained in Batch 1 because of higher-priority blockers.
- Batch 3 / P2: `807` cards. Aliases, manufacturers, sources/confidence,
  regulatory status and usage rules.

`READY_FOR_BATCH_1=YES` means the next task may prepare a read-only,
source-backed review package. It does not authorize merges, component-link
creation or production writes.
