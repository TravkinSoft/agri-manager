# TZ-165 - Execute metadata repair and create A106 Supabase branch

DATE: `2026-07-15`
STATUS: `PARTIAL_PASS_BRANCH_BOOTSTRAP_FAILED`
CORE_BRANCH: `copilot-v1`
PRODUCTION_SCHEMA_CHANGE: `NONE`
PRODUCTION_BUSINESS_DATA_CHANGE: `NONE`

## Metadata repair

TZ-164 commit `5c8927ab074227473b854ee7920ce841270de38f` was pushed to
`origin/copilot-v1`. A fresh production backup was created under
`audit-output/TZ-165/pre-repair-backup/`; manifest SHA-256 is
`ac86d8f0bf9f8dd3778c316b803c6a742385a90f86466072a1ec45dbff9b82ad`.

Live preflight confirmed 76 history rows, exactly 39 approved targets, 37
non-target rows, no old-hash mismatches and the same schema fingerprint as
TZ-164. One guarded transaction updated only
`supabase_migrations.schema_migrations.statements` for the 39 targets. The
second execution changed zero rows. Schema fingerprint and exact counts of all
145 public tables stayed unchanged. Rollback was not required.

## Branch bootstrap

Supabase reported branch cost `$0.01344/hour`; owner approval and cost
confirmation were recorded. Branch `assistant-memory-a106` was created with id
`5263097e-3e68-4300-be91-5f8f941d0e7a` and project ref
`xhlfixtoubejuxnpdzyx`. It reached `MIGRATIONS_FAILED` and was deleted after
logs were captured, stopping further hourly cost.

The first real migration failure occurred in history version
`20260413182000`: its stored statements create trigger
`update_varieties_updated_at` using `public.ensure_updated_at_column()`, but do
not create that helper. The local canonical file does create the helper. This
version was one of the 37 rows intentionally outside the approved repair scope.
The earlier missing `schema_migrations` probe was not terminal; migrations
continued afterward.

## Decision

The 39-row repair remains valid and is not rolled back. A106 remains blocked.
Before another paid branch is created, a separate owner-approved task must
canonicalize the remaining existing history payloads and add the missing
canonical history versions using the already tested 135-row metadata preview,
with fresh backup, guarded hashes, second-run no-op and rollback verification.
No migration SQL may be applied to production.

PHASE STATUS: `PARTIAL_PASS_BRANCH_BOOTSTRAP_FAILED`
METADATA_REPAIR: `PASS`
ROWS_UPDATED: `39`
SECOND_REPAIR_NOOP: `PASS - 0`
PRODUCTION_SCHEMA_CHANGED: `NO`
PRODUCTION_BUSINESS_DATA_CHANGED: `NO`
SUPABASE_BRANCH_CREATED: `YES_THEN_DELETED`
BRANCH_STATUS: `MIGRATIONS_FAILED`
FIRST_BOOTSTRAP_ERROR: `20260413182000 - function public.ensure_updated_at_column() does not exist`
READY_FOR_A106: `NO`
