# TZ-184 - Rebuild pesticide Batch 1 formulation updates

DATE: `2026-07-17`
STATUS: `DONE`
CORE_BRANCH: `copilot-v1`
PRODUCTION_PROJECT: `bhsemlvmkikpntabctml`
PRODUCTION_WRITES: `0`

## Rollback and live baseline

The failed TZ-183 transaction was confirmed to have rolled back completely.
Repeated read-only preflight produced the unchanged production fingerprint
`bf07323460e43153accf2b4cfc29ed265b2238a45f80493864600c9038b241f2`.
No Batch 1, HOLD or unresolved product row changed. Production remains at
`1231` products, `97` product aliases, `6` formulation rows, `431` components,
`1373` component links and `0` company links.

The unrelated dirty TZ-183 API/helper files remain unstaged and are not part of
this task. The reusable Batch 1 generator is the only catalog script in the
TZ-184 commit scope.

## Contract and card review

The exact production constraint `products_product_subcategory_check_v1` is
`NOT VALID`. For pesticide rows it permits `NULL` or one of:

`herbicide`, `fungicide`, `insecticide`, `acaricide`, `desiccant`,
`seed_treatment`, `growth_regulator`, `other`.

The constraint was not changed, relaxed or migrated. The four requested
formulation rows were mapped by exact product UUID:

| Product | Formulation | Legacy subcategory | Canonical subcategory |
| --- | --- | --- | --- |
| Каратэ Зеон | SC | `инсектицид / проверить` | `insecticide` |
| Хакер 300 | SL | `гербицид / проверить` | `herbicide` |
| Золотой Дракон | SL | `гербицид / проверить` | `herbicide` |
| Амплего | CS | `инсектицид / проверить` | `insecticide` |

The full exact update-target audit found five additional invalid legacy values:
Ордан and Фунгоцеб become `fungicide`; the archived duplicate Кассиус, inactive
Black Jack and the inactive safe-review row use `NULL` because no new category
is invented for an archived or out-of-scope identity. Celest Top was already
valid as `seed_treatment`.

## Corrected package

`scripts/catalog/pesticide-batch-one-production.mjs` now:

- validates the exact constraint and all expected source-row states;
- updates formulations and subcategories only by reviewed product UUID;
- excludes HOLD and unresolved cards;
- restores formulation, subcategory and timestamps in the rollback package;
- emits ASCII-only executable SQL while preserving UTF-8 catalog values;
- verifies search behavior, links, repeat safety and exact rollback in PGlite.

The rollback must restore legacy invalid values exactly. Because PostgreSQL
enforces a check on updated rows even when that check is `NOT VALID`, rollback
temporarily drops and recreates the same constraint in one transaction. This
requires an `ACCESS EXCLUSIVE` lock and must only be used by a separately
approved apply task during a controlled window.

## Isolated acceptance

Production-equivalent isolated acceptance passed:

- first apply: `PASS`;
- constraint violations: `0`;
- formulation assignments: `4/4`;
- search regression: `50/50` plus `6/6` controls;
- component links preserved except the approved Smerch salt cleanup;
- company links preserved at `0`;
- HOLD and unresolved rows unchanged;
- second apply: exact no-op with `0` changes;
- rollback: exact baseline fingerprint and timestamps restored.

External evidence is stored outside Git at
`C:/Users/TRAVKIN/Downloads/CodecSaaS/audit-output/TZ-184/`. Its manifest
verifies the five required JSON artifacts. Apply SQL SHA-256 is
`33e98e5aae8f9ccbe17e0b7c48fd665df98497e66a672c62a960adeea7e74f0e`;
rollback SQL SHA-256 is
`d477b1db1731f692d7dc947cbf3aa63dc15a3203ffe27b3e74f296a6a26a3164`.

## Decision

`READY_TO_RETRY_BATCH_1=YES`, but TZ-183 must not be rerun. A new numbered,
owner-approved apply task must repeat fresh backup, manifest verification and
live no-drift preflight before any production write.
