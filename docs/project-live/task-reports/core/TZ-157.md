# TZ-157 - Canonicalize legacy vehicle migration 20260509142000

DATE: `2026-07-14`
STATUS: `PASS_CANONICALIZED`
CORE_BRANCH: `copilot-v1`
PRODUCTION_CHANGE: `NONE`

## Root cause and correction

The local migration used `unique (lower(name))` and another expression inside a table UNIQUE constraint. PostgreSQL does not allow expressions there, so clean replay failed at parser time.

The corrected file uses expression unique indexes and reproduces only the current production-compatible legacy vehicle surface. The active global transport catalog remains `transport_models`; old catalog/company seeds, triggers and superseded checks are intentionally not replayed.

## Verification

- Migration file: `supabase/migrations/20260509142000_personnel_vehicles_master_upgrade.sql`.
- New migration created: NO.
- PostgreSQL 15 parser: PASS for all `135/135` local files and `2524` statements.
- Isolated first apply: PASS.
- Isolated second apply: PASS.
- Duplicate guard: PASS for case-insensitive brand and same-brand model names.
- Valid rows: PASS, 2 brands and 3 models.
- Sentinel business data: unchanged.
- Relevant columns, expression indexes, checks and production-named FKs: match the production contract.
- Dependencies: PASS; later RLS still has the required compatibility tables, while later canonical model links continue to use `transport_models`.

## Required report

PHASE STATUS: `PASS_CANONICALIZED`
CURRENT_BRANCH: `copilot-v1`

MIGRATION_FILE: `supabase/migrations/20260509142000_personnel_vehicles_master_upgrade.sql`
ROOT_CAUSE: `Expression calls were placed in table UNIQUE constraints; the same migration also mixed the required compatibility schema with superseded catalog and company seed behavior.`
LOCAL_FILE_FIXED: `YES`
NEW_MIGRATION_CREATED: `NO`

PARSER_TEST: `PASS - 135/135 files, 2524 statements`
FIRST_APPLY: `PASS`
SECOND_APPLY: `PASS`
DUPLICATE_GUARD: `PASS`
SCHEMA_MATCH_PRODUCTION: `PASS for the relevant compatibility columns, indexes, checks and FK names`
DEPENDENCY_CHECK: `PASS - transport_models stays canonical; empty legacy tables remain for compatibility and later RLS`

PRODUCTION_CHANGED: `NO`
BUSINESS_DATA_CHANGED: `NO`
MIGRATION_APPLIED: `NO`

FILES_CHANGED: `one migration plus docs/project-live/**; audit evidence remains outside commit`
COMMIT_CREATED: `YES - SELF`
COMMIT_HASH: `SELF`
PUSH_DONE: `NO`

READY_FOR_38_HISTORY_RECOVERY: `YES - for a separate exact recovery preview, not execution`
BLOCKERS: `TZ-154 and TZ-A106 remain blocked until the independent 38-row migration-history recovery is approved and completed.`
NEXT_ACTION: `Prepare and review the exact 38-row history-only recovery package. Do not replay migration SQL.`

## Plain Russian summary

1. В старом файле выражение `lower(name)` было ошибочно помещено внутрь ограничения UNIQUE, которое принимает только имена колонок.
2. Ошибка заменена корректными expression unique indexes; устаревшие seed-данные и старая архитектура не возвращаются.
3. Нужные пустые compatibility-таблицы, колонки, индексы и связи соответствуют текущему production-контракту.
4. Рабочая база, бизнес-данные и журнал миграций не изменялись.
5. Можно переходить к отдельной подготовке восстановления 38 записей журнала, но A106 остаётся заблокирован до фактического завершения recovery.
