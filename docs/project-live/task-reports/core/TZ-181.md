# TZ-181 - Pesticide Batch 1 source-backed review

DATE: `2026-07-17`
STATUS: `DONE`
CORE_BRANCH: `copilot-v1`
PRODUCTION_PROJECT: `bhsemlvmkikpntabctml`
MODE: `READ_ONLY_PRODUCTION_REVIEW_PREVIEW`

## Scope and safety

The input was locked to the verified TZ-180 `BATCH_1_P0` set. Exactly `45`
unique product IDs were reviewed; duplicate rows and unclassified rows are
both `0`. The TZ-180 manifest and audit fingerprint
`2d74a26055cbad5a1466b591dc77ffc2926364ccafa06cf2d26f44f05401f696`
were verified before the review.

Production remained read only. Before and after snapshots matched exactly at
`431` components, `63` component aliases, `318` component sources, `1373`
product-component links and `0` company links. Product merge, component link
insert, alias insert, migration, deploy and production write counts are `0`.

## Classification

Every card has one primary status and one review group.

| Group | Cards |
| --- | ---: |
| SAFE_AUTO_APPLY | 20 |
| OWNER_APPROVAL_REQUIRED | 10 |
| UNRESOLVED | 15 |

The safe preview contains `17` source-backed component links across `11`
cards and `4` assignments to existing canonical formulation rows. One of the
component actions is a new canonical `Prometryn` component preview; its exact
active-component and alias identities have zero live collision candidates.
All other component actions reuse existing active component IDs.

Critical facts proposed for the safe package are supported by a manufacturer,
official label or Kazakhstan registry source. No component or formulation was
guessed. Cards without sufficient product-specific evidence remain
`UNRESOLVED` and have no composition/formulation apply action.

## Search review

The TZ-180 search failures contained `50` scenarios across `24` cards. The
minimal preview does not insert aliases. It extends the read path with already
stored `products.name_ru`, `products.name_en` and
`global_product_aliases.alias` values.

All `50/50` target-card scenarios and the Curamin/Phomazin RU/EN control suite
pass in the preview. `30` scenarios are unambiguous. `20` scenarios across ten
RU queries match more than one global product row and therefore require a
user-facing disambiguation result rather than an automatic first match:
`Борей`, `Гардо Голд 500`, `Гербитокс`, `Колосаль Про`, `Ластик Экстра`,
`Милагро плюс`, `Оплот Трио`, `Парадокс`, `Трибьют`, `Эфир 960`.

## Identity decisions

`Celest Top` and `Селест Топ, КС` have the same source-backed composition and
represent one likely product identity. Automatic merge remains forbidden. The
owner must select a survivor after a fresh ID-scoped snapshot; the non-survivor
label should be preserved as an alias only after explicit approval.

The owner queue also contains short or model-sensitive identities such as
`Дитан`, `Smerch VR`, `Метамил`, `Ордан`, `Фунгоцеб`, `Black Jack`, `Кассиус`
and `Курзат`. No identity merge was executed.

## Acceptance and outputs

- reviewed: `45`;
- unclassified: `0`;
- duplicate review rows: `0`;
- component duplicate preview: `0`;
- alias conflict preview: `0`;
- merges executed: `0`;
- production writes: `0`;
- search expected pass: `50/50`;
- review fingerprint:
  `49774e39ff293e24490b0c7ae90d7b23e7c88b10f845eeaf12b6846242a9e580`.

All required external artifacts are at:

`C:/Users/TRAVKIN/Downloads/CodecSaaS/audit-output/TZ-181/`

Two complete runs produced the same review fingerprint and byte-identical
manifest SHA-256. The package is ready for owner review, but production apply
is not authorized. A later apply task must select only the approved safe
subset, take a fresh ID-scoped backup and repeat live no-drift preflight.
