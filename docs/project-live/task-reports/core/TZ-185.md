# TZ-185 - Apply corrected pesticide Batch 1 in production

DATE: `2026-07-17`
STATUS: `DONE`
CORE_BRANCH: `copilot-v1`
OWNER_APPROVAL: `APPROVED`
PRODUCTION_PROJECT: `bhsemlvmkikpntabctml`

## Backup and no-drift preflight

A fresh read-only snapshot and exact rollback package were created before the
production write at:

`C:/Users/TRAVKIN/Downloads/CodecSaaS/audit-output/TZ-184/backups/pesticide-batch-one-20260717T143023716Z/`

The 12-file backup manifest and both executable SQL hashes verified. The live
fingerprint remained
`bf07323460e43153accf2b4cfc29ed265b2238a45f80493864600c9038b241f2`.
Preflight confirmed `1231` products, `97` product aliases, `6` formulations,
`431` components, `318` sources, `1373` component links, `1373` legacy links
and `0` company links. The exact `products_product_subcategory_check_v1`
constraint remained unchanged and `NOT VALID`; waiting locks and active
duplicate component links were `0`.

## Production transaction

The SHA-256 verified TZ-184 SQL was applied once in its single explicit
transaction. It processed all `20` SAFE_AUTO_APPLY cards and the seven
owner-approved physical cards. Exact result metrics totalled `63` changed rows
across products, links, sources, aliases and transferred references.

The resulting catalog state is:

- scoped cards processed: `27`;
- physically updated product rows: `11`, all inside the approved product scope;
- legacy product subcategories normalized: `9/9`;
- source-backed formulation assignments: `4/4`;
- component links added: `17`;
- component sources added: `18` including the owner-verified Smerch source;
- product aliases added: `3`;
- new canonical Prometryn component: `1`.

Owner decisions were applied as approved:

- Celest Top remains active as survivor, with the canonical CS formulation;
  `Селест Топ, КС` is archived and linked to the survivor without company-link
  loss.
- Smerch VR has one active ingredient, glyphosate `540 g/L`, with `potassium
  salt` recorded only as equivalent basis; the separate salt links were
  removed.
- short Ордан, Фунгоцеб and Кассиус rows are archived and point to their full
  survivors; their short names are survivor aliases.
- Black Jack is inactive, not deleted and not archived.

The HOLD cards Дитан, Метамил and Курзат and all 15 unresolved cards retain
their exact pre-apply fingerprints. Company data and company links were not
changed.

## Verification and repeat safety

The original live verifier reported `48/50` because it still expected two
Black Jack searches to return an active card. The owner-approved behavior is
the opposite: Black Jack must be hidden after deactivation. The verifier was
aligned with the already-tested isolated `HIDDEN_INACTIVE` rule and then
passed without another production write.

Final acceptance:

- search regression: `50/50`;
- Curamin/Курамин/Фолиар and Phomazin/Фомазин controls: `6/6`;
- product duplicates, alias conflicts and active component-link duplicates:
  `0/0/0`;
- company links lost: `0`;
- timestamps changed outside the exact product scope: `0`;
- second application: exact no-op, `0` inserts/updates/deletes;
- post-apply fingerprint after both runs:
  `d1a340df99bbe8f7aec4dbd093074d1816fabba874868afc4a24c541547e3eeb`.

Final counts are `1231` products, `100` aliases, `6` formulations, `432`
components, `336` component sources, `1389` component links, `1372` legacy
links and `0` company links. Rollback was not required. The isolated exact
rollback remains verified, including timestamps and the original fingerprint.

## Production health

Supabase reports `ACTIVE_HEALTHY`. `/references`, `/operations`,
`/crop-structure`, `/warehouses/requests`, `/weighbridge` and the pesticide
catalog route returned HTTP `200`. Typecheck and production build passed. No
migration, db push, application deploy or master merge was performed.

## Decision

`READY_FOR_NEXT_CATALOG_BATCH=YES`. The next catalog task may begin from the
new verified Batch 1 production baseline. HOLD and unresolved cards remain out
of scope until separately resolved and approved.
