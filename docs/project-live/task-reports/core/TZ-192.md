# TZ-192 - Integrate Travkin Assistant into Core preview

DATE: 2026-07-18
STATUS: PASS
CORE_BRANCH: `copilot-v1`
CORE_BASE: `de5b618abd6e81940ef940f83203da6e30a60227`
ASSISTANT_SOURCE: `origin/assistant-v1@164ade7233c27855e1568decfdf729ab12448204`
INTEGRATION_COMMIT: `d2b0af1373d8945f297ddf881786278e8a948116`
PREVIEW: `https://agri-manager-4jlutj8ev-travkin-ais-projects.vercel.app`
TEST_BRANCH_REF: `gsglkmudcwkdetqtocae`

## Integration

- `git fetch origin` completed. No merge or rebase was used.
- The A108 integration manifest was read from `origin/assistant-v1` and its exact 56-path preview scope was transferred.
- Forty-three files were copied from the verified Assistant source, twelve shared files received the manifest delta, and `package.json` received only the eight approved A108 QA commands.
- The documented semantic patch for `lib/assistant/v1/engine.ts` was applied. All four manifest conflicts are resolved.
- Assistant audit output, env files, secrets, temporary files, migrations, and unrelated Core/Assistant changes were excluded.

## Static and local acceptance

- Typecheck: PASS.
- Safe production build: PASS. Bundle scan found zero production Supabase references and the exact QA branch reference.
- A106 real memory regression: PASS `10/10`; greeting regression: PASS `4/4`.
- A107 real ERP regression: PASS `45/45`; cross-company leaks `0`; ERP mutations `0`; production connections `0`.

## Preview environment

- The first Git preview exposed a pre-existing Vercel configuration problem: QA branch Auth could not log in because Supabase variables were inherited from the production project environment.
- Preview-only variables were added for Git branch `copilot-v1` using branch ref `gsglkmudcwkdetqtocae`.
- `SUPABASE_SERVICE_ROLE_KEY` was removed from Preview scope and remains Production-only. No service credential was transferred or used.
- The first redeploy failed only because Google Fonts timed out. The unchanged retry built successfully and reached `READY`; no code workaround was introduced for that transient external failure.

## Authenticated functional smoke

- Dashboard: `8` fields, `1000 ha`, `9` crop structure lines, `2` warehouses, `5` operations.
- Stock: ammonium nitrate `1550 kg`, Curamin Foliar `520 l`, Phomazin `200 l`.
- Crop structure: eight field cards and nine lines; no stale/foreign-company data.
- Warehouses: two warehouses, read-only mode, balances and stock details load without service-credential errors.
- Operations: five rows; the `Field28` operation card opens and shows `Potato / Gala / 1 reproduction` plus Curamin Foliar.
- Travkin Copilot answered the authenticated read-only question with the grounded result `8 fields / 1000 ha`.
- Existing server-owned conversation history loaded, while A106 separately proved Memory Policy V2 behavior on the same branch.
- Error banners `0`, mojibake `0`, company-B exposure `0`, ERP writes `0`, production connections `0`.

## Safety outcome

- Production Supabase was not connected to or changed.
- Production Vercel was not deployed.
- Master was not merged.
- No migration or ERP database write was performed by this task; the only Assistant persistence during smoke was the expected test-branch chat message.

READY_FOR_OWNER_TEST: YES
