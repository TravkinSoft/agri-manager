# TZ-151 — Import 24 aliases and 295 sources into production GLBD

PHASE STATUS: `DONE`
DATE: `2026-07-14`
BRANCH: `copilot-v1`
PREVIOUS_CORE_COMMIT: `f4a7088e7516ebab42739f2b2277f3b6254e9b48`
SUPABASE_PROJECT: `bhsemlvmkikpntabctml`

## Scope and Assistant sync

- `origin/assistant-v1` was fetched and reviewed at `20117c6739f6ebb2c538e33f073822b8eb1985f3`; latest report is `TZ-A103.md` with 20/20 read-only acceptance.
- A103 changed no GLBD contract, schema or production data. Contract 0.2 remains unchanged; no merge/rebase occurred.
- Owner approval covered only the exact TZ-150 batch: 24 aliases and 295 sources. The 54 blocked source rows were excluded.

## Candidate integrity

All six TZ-150 candidate files matched their recorded SHA-256 values. The immutable candidate manifest is outside Git at `audit-output/TZ-151/import_candidate_manifest.sha256`.

- aliases: `24`;
- sources: `295`;
- blocked source rows included: `0`;
- target components: `279`, all active and non-archived;
- existing aliases/sources before apply: `0/0`;
- exact/semantic candidate conflicts: `0`;
- apply SHA-256: `DBC6C57EB1CC034DE8F1AE749DCA752C1FFD5CBE0F092823BD4AE4C2B6FBD739`;
- rollback SHA-256: `2A5EDD668B97D6E00125DD394F8849AE3FC33952E091615E7EAA5CFE4A15B78D`.

The production apply contains one transaction, one INSERT into `glbd_component_sources`, one INSERT into `glbd_component_aliases`, deterministic IDs and semantic `NOT EXISTS` guards. It contains no UPDATE, DELETE, merge, archive, DDL, component/product-link/company mutation or migration command.

## Backup and live preflight

Verified backup outside Git:

`C:\Users\TRAVKIN\Downloads\CodecSaaS\db-backups\glbd-aliases-sources-tz151-20260713T215635254Z`

It contains the four GLBD table snapshots, product company scope, package component IDs, schema catalog, remote migration history, counts, preflight, exact apply and rollback files. Its SHA manifest verified with zero mismatches before production write. The final evidence manifest SHA-256 is `D46676F9ACCC003462F37103374A63C4918BC494BEF01DCEFC76109F9F14188C`.

Final live preflight PASS:

- components `425`, active `415`;
- product links `1373`;
- company links `0`;
- aliases/sources `0/0`;
- orphan aliases/sources `0/0`;
- blocked rows included `0`.

## Apply and postcheck

The exact approved SQL executed through `supabase db query --linked --file` in one explicit transaction. First execution committed without errors.

- aliases inserted/total: `24/24`;
- sources inserted/total: `295/295`;
- alias/source payload mismatches against approved CSV: `0/0`;
- alias duplicates `0`, source duplicates `0`, alias collisions `0`;
- orphan aliases/sources `0/0`;
- company links `0`;
- components `425`, product links `1373`.

Full sorted JSON snapshots prove `glbd_components`, `glbd_product_components` and product/company scope are unchanged. Names, types, review/archive statuses and product concentrations therefore did not change.

Second execution of the same candidate committed safely and added aliases `0`, sources `0`; all postcheck values stayed equal.

## Search, sources and rollback

- Database identity search resolves canonical/English `propiconazole`, Russian and uppercase `Пропиконазол`, plus hyphenated/unhyphenated `трибенурон-метил`, to one active component each.
- All 295 stored source payloads match the approved CSV. Types: internal existing data `279`, official registry `6`, manufacturer site `7`, official label `3`; all 16 product-specific claims preserved URL/title/scope/confidence/checked date.
- Current app code has no reads from the new alias/source tables. UI search/card cutover is a separate future scope; the import remains valid.
- Production rollback was not executed. The exact-ID rollback contains only the 319 planned IDs and no broad component delete. An isolated test restored aliases/sources from `24/295` to `0/0` and preserved components/product links at `425/1373`.

## Final state

BLOCKED_SOURCES_REMAINING: `54`
MIGRATION_CREATED_OR_APPLIED: `NO`
DEPLOY_OR_MASTER_MERGE: `NO`
BUSINESS_DATA_CHANGED: `NO — only the approved global alias/source metadata rows were inserted`

Next scope: separately review the 54 blocked source rows; separately connect GLBD V2 aliases/sources to UI search and component cards if approved.
