# TZ-147 - Assistant QA identity for A102

DATE: `2026-07-14`
BRANCH: `copilot-v1`
STATUS: `DONE`

## Scope

Created one dedicated confirmed Supabase Auth user for local read-only Assistant A102 validation. The canonical authorization record is one active `profiles` row linked to the only company named `TravkinFlowTest1`.

Approved identity contract:

- display name: `Assistant QA Test1`;
- company: `TravkinFlowTest1` (`88435427-182c-48e1-a77c-9a5b65161ed5`);
- role: `agronomist`;
- `is_owner=false`;
- not `global_admin`;
- not `company_admin`;
- no `company_people` row;
- email confirmed;
- one company assignment through `profiles.company_id`.

## Verification

- target company exact-name matches: `1`;
- duplicate Auth/profile preflight: `0`;
- normal password sign-in: PASS;
- saved access and refresh tokens: local ignored `project-assistant-v1/.env.local` only;
- token values printed or committed: NO;
- token profile read: `agronomist`, `active`, `is_owner=false`, Test1 company;
- active Test1 season read: PASS (`2026`);
- Test1 read-only data: 1 field and 2 crop-structure rows visible;
- cross-company `companies`, `fields`, and `profiles` reads: 0 rows visible;
- business writes during token verification: 0.

The first guarded create attempt omitted the legacy trigger marker `invited_by_company`. The live self-signup trigger created a fallback empty company, so the script rejected the changed business fingerprint and removed the incomplete Auth user/profile. The single empty fallback company was then deleted after proving it had no references. The successful retry used the existing canonical invite metadata, and the final business fingerprint exactly matches preflight: 3 companies, 100 fields, 122 crop-structure rows, 8 operations, 2 warehouses, 5 warehouse requests, 7 inventory transactions, and 4 company people.

## Security boundary

- Company and role are resolved from the server-owned `profiles` row and enforced by RLS/server ACL.
- The QA token has no service-role privileges and cannot read another company.
- No field, operation, warehouse, inventory, company-personnel, or other business record was created or modified.
- No schema, migration, migration-history, application code, or production deployment changed.
- Secrets are not present in this report or Git.

## Rollback plan

Rollback is prepared but not executed:

1. revoke all sessions for the dedicated QA Auth user;
2. delete that Auth user, allowing the profile FK cascade to remove the canonical profile;
3. verify no profile or company-personnel link remains;
4. remove `A102_TEST_ACCESS_TOKEN` and `A102_TEST_REFRESH_TOKEN` from the ignored Assistant `.env.local`;
5. repeat the business fingerprint and cross-company isolation checks.

## Result

`TZ-A102` may continue locally in read-only mode with authenticated user JWT plus RLS. This approval does not permit Assistant writes, direct SQL, migration work, merge, preview deploy, or production deploy.
