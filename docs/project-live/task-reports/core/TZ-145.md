# ТЗ №145 — Принять read-only фундамент Travkin Assistant

TASK: TZ-145
DIRECTION: CORE / ASSISTANT GOVERNANCE
STATUS: DONE
BRANCH: `copilot-v1`
START_COMMIT: `687653447753df7cb3eb5fd1eef3454b5fdac046`
END_COMMIT: `SELF`

GOAL: Официально принять результаты TZ-A100/TZ-A101 в core Project Live, обновить Integration Contract и разрешить следующий локальный read-only этап без merge, database changes или production deployment.

## Reviewed assistant state

- `origin/assistant-v1`: `51e878e7306d0b6a821a21b9a7174466e165d10c`.
- TZ-A100: статический read-only runtime/security audit принят.
- TZ-A101: локальный isolated Assistant V1 foundation принят.
- Assistant Live/Sync state прочитаны через `git show`; merge/rebase не выполнялись.
- Security proposal `2026-07-assistant-p0-core-security.md` рассмотрен и принят как список открытых core P0, но сами маршруты не исправлялись.

## Accepted A101 evidence

- Mocked QA: `16/16` PASS.
- `npm run typecheck`: PASS на assistant branch.
- `npm run build`: PASS с прежним Supabase Realtime warning.
- Production calls: `0`; DB writes: `0`; schema changes: `0`.
- User-JWT/RLS read path, central company/role/season/result policy and thread-scoped state are part of the accepted local foundation.

## Contract 0.2

Allowed model tools:

1. `get_current_context`;
2. `search_fields`;
3. `get_field_card`;
4. `get_field_land_bank_summary`;
5. `get_field_materials`;
6. `get_warehouse_stock`;
7. `get_crop_structure_summary`;
8. `get_active_operations_summary`.

All create tools, draft confirmation, navigation execution, KB mutations, warehouse/operation writes, generic SQL and tools with side effects remain forbidden. Local development and TZ-A102 validation are allowed; merge and production deployment are not.

## Open core P0

1. Legacy `app/api/operations/confirm-draft/route.ts` does not enforce canonical operation-create permissions and season guard.
2. Knowledge Base DELETE does not enforce document `company_id` ownership before service-role mutation.

Both routes remain unreachable and forbidden from Travkin Assistant V1 until separate core fixes and acceptance evidence.

## Registry and safety

- TZ-A100: DONE.
- TZ-A101: DONE at `51e878e`.
- TZ-A102: PLANNED, Real Local Runtime Validation on `assistant-v1`.
- Assistant merged: NO.
- Application code changed by TZ-145: NO.
- DB/schema/business data changed: NO.
- Production/deploy changed: NO.
- Integration Contract changed: YES, `0.1 -> 0.2`.

## Next action

Run TZ-A102 only in the isolated `assistant-v1` worktree after fetching core and recording contract 0.2/hash in Assistant Sync State. Keep production, writes, direct SQL, database changes, merge and deploy disabled.
