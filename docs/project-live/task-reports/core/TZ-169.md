# TZ-169 — Contract 0.4 and Branch-Only Memory Policy V2

Date: 2026-07-16

Branch: `copilot-v1`

Target Supabase branch: `assistant-memory-a106` (`gsglkmudcwkdetqtocae`)

Production project: `bhsemlvmkikpntabctml` (read-only control only)

## Decision

Contract 0.4 replaces the V1 candidate-first behavior with three explicit scopes:

- `USER_GLOBAL`: one user's durable personal preferences across all old and new chats;
- `COMPANY`: same-company rules/terminology/process preferences with an owner/admin/director role guard;
- `CONVERSATION`: chat-local summary, entity and unresolved-question state in versioned message metadata.

An explicit user command creates an atomic direct-approved memory. A safe assistant inference is allowed only for the six documented personal categories with confidence at least `0.850`. Immediate user-global deletion requires no second confirmation and leaves a content-free audit event.

## Proposal provenance

`git fetch origin` completed. The requested proposal path is absent from `origin/assistant-v1` at `b22f765583b2cd556a29b9e25c332561f19dd262`; `git show` therefore could not read it. The exact proposal was reviewed read-only from the local untracked `project-assistant-v1` worktree:

`docs/project-live/contract-proposals/assistant/2026-07-16-owner-memory-behavior-v2.md`

Proposal SHA-256: `053481AE571BEB4E20A3C67F54437F8A3903EC7B82F59F20C141E5D8E3AFA196`. No Assistant file was edited, staged, merged or rebased.

## Migration

Created `supabase/migrations/20260716125205_assistant_memory_policy_v2.sql` and applied it only to `gsglkmudcwkdetqtocae`.

The migration:

- adds `provenance`, `normalized_fact`, `approval_mode`, and scope/provenance audit fields;
- preserves five V1 QA memories as `legacy_v1` and creates no new candidate path;
- adds direct-approved lifecycle/type/provenance constraints;
- adds case-insensitive active-fact dedupe indexes;
- replaces V1 lifecycle/audit triggers with V2 triggers;
- applies USER_GLOBAL ownership and COMPANY role/company RLS;
- removes authenticated `TRUNCATE`, `TRIGGER`, and `REFERENCES` grants;
- preserves content-free deletion audit and expiry support.

Local isolated PostgreSQL test: PASS. Parser, first apply, direct-approved insert, zero candidates, inferred allowlist denial, company role guard and deletion audit all passed.

Branch history advanced `135 -> 136`, head `20260716125205`. Production remained history `135`, head `20260714140722`; production `assistant_memory_events` remains absent.

## Real JWT acceptance

Request-scoped JWT clients for QA users A and B were used. No service role was used by the runtime test.

1. Explicit remember-name creates direct approved memory: PASS.
2. Explicit memory creates no candidate: PASS.
3. Name is available in a new chat and second session: PASS.
4. Name is available when the old chat continues: PASS.
5. Preferred address is available across chats: PASS.
6. Safe stable work preference is stored as assistant-inferred: PASS.
7. Temporary warehouse balance is rejected by the allowlist: PASS.
8. Forget-name deletes immediately and leaves content-free audit: PASS.
9. User B cannot read, update or delete user A memory: PASS.
10. Ordinary agronomist cannot create company-wide memory: PASS.

Additional company guard:

- QA user A was temporarily changed on the test branch from `agronomist` to `company_admin`;
- own-company create/update/delete passed with the real JWT;
- user B cross-company read/update/delete returned denial or zero rows;
- foreign company spoof was denied;
- QA user A was restored to `agronomist` in a guaranteed cleanup step.

Post-cleanup branch counts equal the pre-test business-state counts: memories `5`, chats `10`, messages `120`, company memories `0`, non-agronomist QA profiles `0`. Content-free memory events increased `12 -> 22` as intended acceptance evidence. No ERP, warehouse, operation, GLBD or production data changed.

## A106 boundary

The eight read-only ERP tools, Responses API, `store:false`, GPT-5.6 Terra, reasoning medium, 60-message limit, summary/entity state, ERP-write prohibition and production isolation are unchanged.

A106 may sync Contract 0.4 and continue only on `assistant-memory-a106`. A107 has not started. Production migration, merge and deployment remain disabled.

## Final report

PHASE STATUS: `PASS`

CONTRACT_VERSION: `0.4`

PROPOSAL_REVIEWED: `YES — local untracked Assistant worktree; absent from origin`

USER_GLOBAL_MEMORY: `APPROVED_AND_BRANCH_TESTED`

COMPANY_MEMORY: `APPROVED_ROLE_GATED_AND_BRANCH_TESTED`

CONVERSATION_MEMORY: `APPROVED_CHAT_LOCAL_METADATA`

DIRECT_APPROVED_INSERT: `PASS`

CANDIDATE_CREATED_FOR_EXPLICIT_MEMORY: `NO`

INFERRED_MEMORY_ALLOWLIST: `PASS — 6 safe types, confidence >= 0.850`

IMMEDIATE_DELETE: `PASS_WITH_CONTENT_FREE_AUDIT`

MIGRATION_CREATED: `YES — 20260716125205_assistant_memory_policy_v2.sql`

MIGRATION_APPLIED_TO_TEST_BRANCH: `YES — gsglkmudcwkdetqtocae only`

PRODUCTION_MIGRATION_APPLIED: `NO`

RLS_TESTS: `PASS — real JWT`

CROSS_USER_DENIED: `YES`

CROSS_COMPANY_DENIED: `YES`

COMPANY_ROLE_GUARD: `PASS`

SCENARIOS_TOTAL: `10`

SCENARIOS_PASS: `10`

SCENARIOS_FAIL: `0`

PRODUCTION_CHANGED: `NO`

BUSINESS_DATA_CHANGED: `NO — branch QA rows restored; audit evidence only`

COMMIT_CREATED: `YES — SELF`

COMMIT_HASH: `SELF`

PUSH_DONE: `YES — origin/copilot-v1`

READY_TO_RESUME_A106: `YES_AFTER_CONTRACT_SYNC`

BLOCKERS: `None for A106 branch-only continuation; proposal provenance must be committed by Assistant during its next authorized task`

NEXT_ACTION: `Sync Contract 0.4 into assistant-v1, replace V1 candidate-first runtime behavior, repeat A106 runtime acceptance on gsglkmudcwkdetqtocae; do not start A107.`
