# TZ-187 - Prepare the next safe pesticide batch

DATE: `2026-07-18`
STATUS: `DONE`
CORE_BRANCH: `copilot-v1`
START_COMMIT: `5b5a57c8b8490340c92b72e2a74a4ca4404d4613`
MODE: `READ_ONLY_PRODUCTION_AUDIT_AND_APPLY_PREVIEW`

## Scope and baseline

The audit locked its input to the exact `807` `BATCH_3_P2` cards from TZ-180.
The TZ-180 and TZ-181 manifests and the TZ-180 fingerprint were verified before
the live read. All `807` IDs still exist as global, non-archived pesticide
cards. Batch 1 overlap, company cards and company-data reads/writes are `0`.

The current production read-only baseline contains `848` active global
pesticides and `100` global product aliases. The four-card difference from the
original `852` is consistent with the approved TZ-185 Batch 1 merge/archive
outcome; it does not reduce the locked `807`-card scope.

The current [Supabase changelog](https://supabase.com/changelog) was reviewed.
No current platform change affects this read-only PostgREST catalog audit.

## Safe classification

No collision-free missing alias can be copied from `products.name_ru/name_en`:
the RU values duplicate the trade name and the audited cards do not contain an
independent EN value. No translation was invented.

The safe additive class is a short-name alias obtained by removing exactly one
recognized trailing formulation code (`КЭ`, `КС`, `ВДГ`, `ВР`, `ВРК`, `СК`,
`МКС`, `СП`, `WP`, `EC`, `SC`, `WG`, `SL`, `FS` or `CS`) from the same existing
trade name. The canonical trade name and formulation are not changed.

- safe cards: `564`;
- safe alias actions: `564`;
- batches: `12` (`11 x 50` cards and `1 x 14` cards);
- foreign identity/alias collisions excluded: `8`;
- cards without a proven safe action: `243`;
- manufacturer-prefix candidates: `0`;
- independent formatting-only candidates: `0`;
- original TZ-180 search failures in this 807-card scope: `0`.

The eight collisions remain review-only. Similar spelling is not treated as
evidence for merging products.

## Proposed first batch

Batch 1 contains exactly `50` cards and `50` additive aliases. Examples:

| Current trade name | Alias preview |
| --- | --- |
| Авадекс БВ 48%, КЭ | Авадекс БВ 48% |
| Авторитет, КС | Авторитет |
| Аккурат Экстра, ВДГ | Аккурат Экстра |
| Актара, ВДГ | Актара |
| Амистар Топ, СК | Амистар Топ |
| Баста 15%, ВР | Баста 15% |

The exact fifty-card list is in
`C:/Users/TRAVKIN/Downloads/CodecSaaS/audit-output/TZ-187/batch_01_cards.csv`.
Every action has a deterministic alias ID, exact product ID, normalized alias,
source field and rollback condition.

## Preview verification

- first apply simulation: `50` inserts;
- duplicate alias keys after apply: `0`;
- second apply: `0` changes;
- exact rollback fingerprint: `PASS`;
- two complete audit runs: byte-identical;
- final manifest SHA-256:
  `7486E4BF0D272A4E3EA84673367A996E81F8B811500D7F9ACB1BE588F85579CF`;
- production writes: `0`;
- business/company data changes: `0`.

The external package contains the full 807-card inventory, collision review,
all batch assignments, Batch 1 apply/rollback JSON previews, test results,
owner review and verified manifest.

## Decision

`READY_FOR_OWNER_REVIEW=YES` and `APPLY_READY=NO`. A later numbered task may
prepare or apply Batch 1 only after explicit owner approval, a fresh backup and
live no-drift preflight. Usage rules, registration, composition, the 15
unresolved cards, Дитан, Метамил, Курзат and company data remain out of scope.
