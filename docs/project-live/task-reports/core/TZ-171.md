# TZ-171 - Apply corrected catalog security hardening in production

DATE: `2026-07-16`
STATUS: `PASS_APPLIED`
CORE_BRANCH: `copilot-v1`
OWNER_APPROVAL: `APPROVED`
PRODUCTION_CHANGE: `APPROVED_SECURITY_SCHEMA_ONLY`

## Approved scope

Only migration `20260716114950_catalog_rls_function_security_hardening.sql`
was applied to production. Its SHA-256 matched the approved value:
`1c8d9d7633b2515e45da60609e076984a05247911b556b1009896efd78d27448`.
No Warehouse Units V2, Assistant Memory or other pending migration was applied.

The migration ran inside its explicit `begin`/`commit` transaction and added
only the approved catalog security scope: private authorization helpers, RLS
and command-specific policies for eight tables, deterministic search paths for
27 SECURITY DEFINER functions, and narrowed function/table grants.

## Backup and preflight

- fresh backup: `audit-output/TZ-171/pre-apply-backup/` (outside Git);
- backup entries: `14` plus the SHA-256 manifest;
- manifest SHA-256: `64a2fb211a59c1b7af305e930f588c2fc5d183cad10197ceebad0ec04b981447`;
- pre-apply schema fingerprint SHA-256:
  `e60ac8740c8a76adad8a1b792a2314827047305559e0b156cfedd0d23fdd5a9d`;
- production was `ACTIVE_HEALTHY`, history had `135` versions and the target
  version was absent;
- exactly eight target tables and 27 target functions matched the TZ-170
  baseline;
- target business row counts and complete public-table row counts were saved;
- the approved rollback was present and hash-verified;
- an isolated official CLI workdir excluded later migration
  `20260716125205_assistant_memory_policy_v2.sql`, leaving exactly one pending
  migration: `20260716114950`.

## Apply and post-check

- transaction: PASS;
- migration history: `136` versions, head `20260716114950`, target count `1`;
- Assistant Memory Policy V2 in production history: `0`;
- RLS: `8/8`;
- policies: `32`;
- wide write policies: `0`;
- public SECURITY DEFINER functions: `27`;
- safe search path: `27/27`;
- `PUBLIC` execute: `0`;
- anon execute: `0`;
- authenticated execute: exactly five approved session/browser functions;
- private helper exposure through the Data API: none (`404`);
- target and complete public-table row counts: unchanged;
- tables, columns, constraints, indexes, triggers and views: no unexplained
  differences;
- all fingerprint differences are limited to approved table RLS state,
  functions, policies and table/function grants.

Rollback was not required.

## Real JWT and catalog acceptance

The production matrix used real signed JWTs for ordinary users, warehouse,
company admin and global admin roles. Temporary QA users and fixtures were
removed after the run.

- JWT matrix: `178/178 PASS`, failures `0`;
- cross-company denial: PASS;
- global-admin CRUD: PASS;
- own-company sync: PASS;
- service-role import: PASS in a rolled-back transaction;
- temporary users deleted: PASS;
- Auth user count and public row counts restored exactly;
- catalog reads for products, crops, seed reproductions, aliases, suppliers
  and crop categories: PASS;
- ordinary-user staging visibility: zero rows;
- critical production routes: `8/8 HTTP 200` (`/auth/login`, `/dashboard`,
  `/references`, `/operations`, `/crop-structure`, `/warehouses/requests`,
  `/weighbridge`, `/platform`).

## Boundaries and readiness

Production business data, app deployment and `master` were not changed. The
non-production `assistant-memory-a106` branch remained `ACTIVE_HEALTHY` and was
not used, rebased, reset or modified. Existing advisor findings outside the
approved 8-table/27-function scope remain separate P1 work and are not a TZ-171
blocker.

Catalog security hardening is complete and Core may return to GLBD work.
