# TZ-179 - Production selective apply component package

DATE: `2026-07-17`
STATUS: `DONE`
CORE_BRANCH: `copilot-v1`
OWNER_APPROVAL: `APPROVED`
PRODUCTION_PROJECT: `bhsemlvmkikpntabctml`

## Backup and preflight

A fresh external backup was created before the production write at:

`C:/Users/TRAVKIN/Downloads/CodecSaaS/audit-output/TZ-179/backups/glbd-components-20260717T103351595Z/`

It contains full component, alias, source, product-component and product
snapshots, statuses, timestamps, fingerprints, the exact rollback package and a
verified SHA-256 manifest. The baseline was exact: `425` components, `24`
aliases, `295` sources, `1373` product links and `0` company links.

Both the retained TZ-178 package manifest and the TZ-179 execution manifest
verified successfully. The executable SQL was ASCII-only, contained one
`BEGIN` and one `COMMIT`, and had zero replacement characters or non-ASCII
bytes. Live preflight found all `53` approved rows, excluded Humic acids, and
confirmed the two expected legacy safener identities were explicitly relinked
and archived rather than left as duplicates.

## Transactional apply

The approved package was applied once to production in its explicit
transaction. Exact decision scope:

- direct attachments: `3`;
- SAFE_ALIAS decisions: `33`;
- physical alias rows: `39`;
- safeners: `6`;
- inactive decisions: `10`;
- rejected source: `1`;
- Humic acids changes: `0`.

The package inserted `6` canonical safeners, `39` aliases and `23` component
sources, normalized the exact approved global product-component rows and
archived the exact reviewed legacy rows. It did not update `products`, company
tables or product trade names.

## Post-apply acceptance

Final production counts are:

| Object | Before | After |
| --- | ---: | ---: |
| components | 425 | 431 |
| aliases | 24 | 63 |
| sources | 295 | 318 |
| global product links | 1373 | 1373 |
| company links | 0 | 0 |

Post-check results:

- processed decisions: `53/53`;
- component duplicates, alias conflicts and active link duplicates: `0/0/0`;
- garbage components and mojibake values: `0/0`;
- all six safeners have exact Russian and English labels and valid safener
  product roles;
- RU and EN runtime search passed for all six safeners;
- API JSON round-trip and the real `component-discovery` UI display helper
  preserved Unicode;
- all reviewed inactive rows are hidden from ordinary search and remain visible
  to raw Global Admin catalog reads;
- the product snapshot and Humic-acids row match the pre-apply backup exactly.

The exact package was applied a second time. Counts and the full data
fingerprint remained unchanged at
`d58c6ae8c277bcd350764199cf9ed38c1cf7ed4fe16cff3503c10dc7cfab9bc4`,
proving a true no-op with zero inserts, updates or deletes.

## Production health

The component catalog, `/references`, `/operations`, `/crop-structure`,
`/warehouses/requests` and `/weighbridge` returned final HTTP `200`.
Production remains `ACTIVE_HEALTHY`. Rollback was not required. No migration,
db push, deploy or master merge was performed.

## Decision

`READY_FOR_NEXT_GLBD_STAGE=YES`. The verified production baseline is now
`431/63/318/1373`, company links remain `0`, and Humic acids remains explicitly
`HOLD_OUT_OF_SCOPE`.
