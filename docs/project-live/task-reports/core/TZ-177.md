# TZ-177 - Synchronize production catalog security to Assistant QA branch

DATE: `2026-07-17`
STATUS: `PASS_APPLIED_BRANCH_ONLY`
CORE_BRANCH: `copilot-v1`
TEST_BRANCH: `assistant-memory-a106 / gsglkmudcwkdetqtocae`
PRODUCTION_CHANGE: `NONE`

## Approved scope and preflight

Only `20260716114950_catalog_rls_function_security_hardening.sql` was
approved for the Assistant QA branch. Its SHA-256 matched the approved value:
`1c8d9d7633b2515e45da60609e076984a05247911b556b1009896efd78d27448`.

The branch was `ACTIVE_HEALTHY`, the target history version was absent, all
eight target tables had RLS disabled and no target policies existed. A real
User A JWT could update a fixture alias. Production was `ACTIVE_HEALTHY` and
already had the canonical state: RLS `8/8`, policies `32`, hardened functions
`27/27`, and `PUBLIC`/anon execute `0/0`.

The ERP Ground Truth matched before apply: eight company-A fields totalling
1000 ha, nine crop-structure lines, five operations, two company-A warehouses,
one company-B warehouse and seven ledger rows. Balances were exactly 1550 kg
ammonium nitrate, 520 l Curamin Foliar and 200 l Phomazin for company A, plus
an isolated 777 kg ammonium-nitrate balance for company B.

## Branch-only migration apply

The repository remained linked to production and was not relinked. An isolated
temporary CLI workdir outside Git mirrored the 136 versions already present in
the branch and contained the approved target file as its only pending version.
The first minimal-workdir attempt stopped before SQL because remote history
files were not present locally. No repair was run and no database change was
made by that attempt.

The verified workdir contained 137 canonical local files, 136 matching remote
versions, zero remote versions missing locally and exactly one pending version:
`20260716114950`. Official `supabase migration up --include-all --db-url ...`
then applied only that version to `gsglkmudcwkdetqtocae`. No `db push`, other
migration, branch reset, merge or deploy was executed.

## Security acceptance

Post-apply branch state:

- RLS enabled: `8/8` target tables;
- policies: `32` (`4` per target table);
- public SECURITY DEFINER functions: `27`;
- deterministic safe search path: `27/27`;
- `PUBLIC` execute: `0`;
- anon execute: `0`;
- authenticated execute: exactly five approved browser/session functions.

Real JWT acceptance passed:

- QA User A and QA User B can SELECT global aliases but cannot INSERT, UPDATE
  or DELETE them;
- User A and User B retain exact own-company reads and zero cross-company
  fields, warehouses, operations, ledger, chats and memories;
- a temporary real-JWT company admin could read aliases and its own eight
  fields, saw zero company-B fields, and could not mutate aliases;
- a temporary real-JWT global admin completed alias INSERT, SELECT, UPDATE and
  DELETE;
- anon SELECT, INSERT, UPDATE and DELETE were denied;
- all temporary Auth users, profiles and aliases were removed; residue is `0`.

`SECURITY_GATE=PASS`.

## Dataset and production boundaries

Post-apply Ground Truth is unchanged: fields `8`, area `1000 ha`, crop lines
`9`, operations `5`, warehouses `2+1`, ledger rows `6+1`, and balances
`1550 kg / 520 l / 200 l` for company A plus isolated `777 kg` for company B.
The QA dataset was not recreated.

Production received only read-only health and fingerprint checks. It remains
`ACTIVE_HEALTHY`, with migration-history count `136`, target version count `1`
and target RLS `8/8`. Production schema, Auth, business data, app deployment
and `master` were not changed; production writes are `0`.

The branch Security Advisor still reports pre-existing findings outside the
approved 8-table/27-function catalog scope. They remain separate P1 review work
and do not invalidate this exact security gate.

## Decision

The production-equivalent catalog security gate is now synchronized only to
`assistant-memory-a106`. Contract 0.4, Assistant Memory Policy V2 and the ERP QA
dataset remain intact. `TZ-A107` may begin only on this isolated branch.
Production Assistant migration, memory writes, merge and deploy remain
disabled.
