# TZ-166 - Complete canonical migration history and retry branch bootstrap

DATE: `2026-07-16`
STATUS: `PASS_WITH_EXPLAINED_BRANCH_SCHEMA_DELTA`
CORE_BRANCH: `copilot-v1`
PRODUCTION_CHANGE: `MIGRATION_METADATA_ONLY`

## Metadata repair

The owner-approved repair was executed in one guarded transaction against only
`supabase_migrations.schema_migrations`. A fresh backup was verified at
`audit-output/TZ-166/pre-repair-backup/`; its manifest SHA-256 is
`c905087d55e8ced333e2802bcb51b97c1affaab06803b29db23c6057ceb090a2`.

- history rows before: `76`;
- already canonical and unchanged: `39`;
- existing payloads canonicalized: `37`;
- missing canonical versions inserted: `59`;
- history rows after: `135`;
- canonical hashes: `135/135`;
- second full repair: `0` changes;
- duplicate versions and name/version mismatches: `0`;
- migration SQL executed: `NO`;
- production schema and business-count fingerprints: unchanged.

The rollback package restores the exact post-TZ-165 state: it restores the 37
old payloads and removes only the 59 rows whose inserted hashes still match.
The already repaired 39 rows are preserved. Rollback was not required.

## Branch bootstrap

A new data-less Supabase development branch was created successfully:

- name: `assistant-memory-a106`;
- branch id: `4ee1d1a5-0cdd-490f-aa8c-282a6244749f`;
- project reference: `gsglkmudcwkdetqtocae`;
- status: `FUNCTIONS_DEPLOYED` / `ACTIVE_HEALTHY`;
- migrations processed: `135/135`;
- first bootstrap error: `NONE`.

The branch contains `chats`, `chat_messages`, `assistant_memories` and the
foundation-created `assistant_memory_events`. RLS is enabled on all four;
assistant memory lifecycle policies, indexes and foreign keys are present.
The event table was created only by the canonical branch migration, not by a
manual SQL action.

## Schema comparison and gate

The production-head canonical replay through version 133 still matches the
production semantic catalog with zero unexplained differences. The full branch
is not byte-for-byte or object-for-object identical to production because it
correctly applied two later non-production candidates:

- `20260713183038_warehouse_canonical_units_v2`;
- `20260714140722_assistant_memory_foundation_v1`.

Every observed branch-only object maps to those two files; unexplained schema
differences are `0`. This expected delta is suitable for isolated A106 testing,
but it exposes a controlled metadata risk: production migration history now
lists those two versions although their schema remains intentionally unapplied
there. Before a future production `db push` or automated migration run, those
two versions require a separate owner decision. No correction was made here.

Legacy permissive public policies on `chats` and `chat_messages` also remain on
the branch. The new owner/company policies do not by themselves prove denial
because PostgreSQL permissive policies are combined with OR semantics. Contract
0.3 already assigns real-JWT isolation testing and any branch-only policy fix to
TZ-A106. Production RLS was not changed.

## Result

PHASE STATUS: `PASS_WITH_EXPLAINED_BRANCH_SCHEMA_DELTA`
HISTORY_ROWS: `76 -> 135`
ROWS_UPDATED: `37`
ROWS_INSERTED: `59`
SECOND_REPAIR_NOOP: `PASS - 0 changes`
PRODUCTION_SCHEMA_CHANGED: `NO`
PRODUCTION_BUSINESS_DATA_CHANGED: `NO`
BRANCH_BOOTSTRAP: `PASS - 135/135`
SCHEMA_MATCH_PRODUCTION: `NO - two explained non-production migration scopes`
UNEXPLAINED_SCHEMA_DIFFERENCES: `0`
READY_FOR_A106: `YES - branch-only implementation and real JWT/RLS acceptance`
PRODUCTION_MEMORY_WRITES: `DISABLED`
MASTER_MERGE: `NO`
DEPLOY: `NO`
