# TZ-150 — GLBD Component aliases and sources import preview

PHASE STATUS: `DONE_WITH_BLOCKED_ROWS`
DATE: `2026-07-14`
BRANCH: `copilot-v1`
PREVIOUS_CORE_COMMIT: `b42d777ad9333bc11ed9adfe8b732bb0f72dc6c1`
SUPABASE_PROJECT: `bhsemlvmkikpntabctml`

## Scope and safety

- `origin/assistant-v1` was fetched and read through `git show`; latest commit remains `c4ec0b041b6486d0a3af6d759597c05129d0a470` (`TZ-A102`).
- Assistant contract and GLBD interfaces did not change. No merge, rebase or Assistant code edit occurred.
- Production was queried read-only. Guarded PostgREST made `7` read requests and `0` non-read requests.
- Production import, DB write, migration, `db push`, component merge/archive, product-link update, company-data change, deploy and master merge: `0`.
- All generated CSV/SQL/review files are outside Git at `C:\Users\TRAVKIN\Downloads\CodecSaaS\audit-output\TZ-150`.

## Input integrity

The eight TZ-149 files were the only inputs. Their SHA-256 values were recomputed before processing.

| Input | Rows | SHA-256 |
| --- | ---: | --- |
| `glbd_components_current_snapshot.csv` | 425 | `E796A054957648100EF28193392969411591CFBF426164F0B342E8857B03C811` |
| `glbd_component_aliases_master.csv` | 528 | `1C7AEA3159A24425DBDFF6349CABE1DC3FA615A8B029CCCFA709CABEA631CB4D` |
| `glbd_component_sources_master.csv` | 441 | `9740D3441D8042E2AA6FF5A3F767D571A301DD682C3070725E60EE1586FDB264` |
| `glbd_product_component_source_gaps.csv` | 1373 | `5CDA78F57173FF4B33C6EE2EA950258DC32E73497AC4CC7F078594756C54EE98` |
| `glbd_component_identity_conflicts.csv` | 172 | `6556740CA8F59F35B94DBF37DB73E4490DF2BA3B96E1E6FE87904965D3F44C51` |
| `glbd_component_owner_review_queue.csv` | 172 | `B7F1124F89E1B192F5A71E3FFB79B0183567A5B37EF79DE08EE5A9C0701991E0` |
| `glbd_component_alias_source_plan.md` | 56 lines | `647D26C97F70E1EE62EA1F3F311E5E2015779890B60E56F0756FD3E07919DE72` |
| `glbd_component_alias_source_selfcheck.md` | 37 lines | `D0E8B2D312DA8BAA570C0FE5F6F6B67C65CF8597435735E496789ECDCBF56DE2` |

Expected input counts matched: `READY_FOR_ALIAS_IMPORT=24`, `READY_FOR_SOURCE_IMPORT=349`.

The complete component/link ID sets and identity/status fields matched live production. Live baseline remained components `425`, active components `415`, product links `1373`, aliases `0`, sources `0`, company links `0`, non-draft components/links `0/0`.

## Alias validation

- Input aliases: `24`.
- SAFE_TO_IMPORT: `24`.
- Blocked: `0`.
- Collisions with existing aliases, other active-component identities or another input candidate: `0`.
- Empty alias/normalized text, invalid language/type/confidence, canonical no-op, form relation, concentration/number garbage and disputed queue overlap: `0`.
- Every alias has a deterministic planned ID and references the exact bounded internal identity-source row for the same component.

## Source validation

- Input sources: `349`.
- SAFE_TO_IMPORT: `295`.
- Blocked: `54` distinct rows.
- Safe types: `internal_existing_data=279`, `official_registry=6`, `manufacturer_site=7`, `official_label=3`.
- Exact existing/input semantic duplicates: `0`.
- Invalid URL, title, confidence, checked_at or vague claim scope among the safe subset: `0`.
- All 16 external rows match a source-verified global product/component link, explicit concentration and bounded formulation claim.
- Blocked overlaps: merge review `20`, safener review `21`, garbage/relink `1`, needs-source `3`, inactive component `10`. One row belongs to more than one review category, so category totals are not a distinct-row sum.

The strict result differs from the TZ-149 headline `349`: 44 rows also belong to explicit owner-review queues and another 10 target inactive components. They are preserved in `glbd_sources_blocked.csv` and excluded from every import VALUES list.

## Preview package

Required files created outside Git:

1. `glbd_aliases_import_ready.csv` — 24 rows.
2. `glbd_sources_import_ready.csv` — 295 rows.
3. `glbd_aliases_blocked.csv` — 0 rows plus header.
4. `glbd_sources_blocked.csv` — 54 rows.
5. `glbd_aliases_sources_import_preview.sql`.
6. `glbd_aliases_sources_rollback_preview.sql`.
7. `glbd_aliases_sources_preflight.sql`.
8. `glbd_aliases_sources_postcheck.sql`.
9. `glbd_aliases_sources_import_report.md`.
10. `glbd_aliases_sources_owner_review.md`.

SQL safety:

- import/rollback are not migrations and fail closed without exact batch-specific session tokens;
- import DML is limited to one INSERT into `glbd_component_sources` and one INSERT into `glbd_component_aliases`;
- rollback DML is limited to exact-ID/payload DELETE from aliases, then sources;
- preflight/postcheck contain no DML;
- deterministic IDs plus semantic NOT EXISTS checks make the preview repeat-safe;
- blocked IDs present in import SQL: `0`;
- no broad DELETE by `component_id` exists;
- `glbd_components`, `glbd_product_components`, products and company data are never written.

## Isolated PostgreSQL test

An in-memory PGlite/PostgreSQL database was loaded with the confirmed snapshot: 425 components, 825 global products, 1373 product links, aliases 0, sources 0.

- Import without the exact token: blocked before DML.
- First approved isolated apply: aliases `24`, sources `295`.
- Second apply: aliases `24`, sources `295`; new rows `0/0`.
- Alias duplicate groups: `0`.
- Source duplicate groups: `0`.
- Orphan aliases/sources: `0/0`.
- Company links: `0`.
- Components/product links: unchanged at `425/1373`.
- Archive and review-status fingerprints: unchanged.
- Rollback without the exact token: blocked before DML.
- Exact approved isolated rollback: aliases/sources restored to `0/0`; components/product links remained `425/1373`.

## Decision

TECHNICALLY_IMPORTABLE_SUBSET: `24 aliases / 295 sources`
BLOCKED_SOURCE_ROWS: `54`
READY_FOR_PRODUCTION_IMPORT: `NO`

This task provides no production apply approval. Before any future apply, the owner must explicitly accept the reduced 24/295 batch, leave the 54 rows blocked, request a fresh backup/live preflight, and approve the exact guarded SQL hash. Resolving the 54 rows belongs to separate merge/safener/garbage/source/inactive-component review work.

DB_CHANGED: `NO`
BUSINESS_DATA_CHANGED: `NO`
IMPORT_EXECUTED: `NO`
MIGRATION_CREATED: `NO`
