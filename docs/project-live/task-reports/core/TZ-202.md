# TZ-202 - Full pesticide research export

STATUS: `PASS`

DATE: `2026-07-19`

BRANCH: `copilot-v1`

SOURCE HEAD: `193cee7c6ee5dfa704a2d3564e5594f2b2cabb50`

## Scope

The task produced a read-only external research package for every existing
global pesticide card. The production catalog supplied the 852-card baseline;
the isolated QA branch `gsglkmudcwkdetqtocae` supplied only the ten already
verified Full Pesticide Card V1 pilot records. No company-scoped product or
business row was included.

The reusable exporter is
`scripts/audit/export-pesticide-full-research-pack.mjs`. It uses select-only
Supabase clients, rejects unexpected project refs, requires exactly 852 unique
global pesticide IDs and stops on company data, duplicate IDs, broken Unicode
or a changed second-read fingerprint.

## Export result

- products: `852`;
- unique product IDs: `852`;
- duplicate export rows: `0`;
- company products: `0`;
- product aliases: `632`;
- composition rows: `1385`, with one concentration/unit object per link;
- existing Full Card V1 registrations: `6`;
- existing Full Card V1 usage rules: `9`;
- confirmed QA pilot cards preserved: `10`;
- broken Unicode values: `0`.

Every card contains separate normalized Full Card V1 fields and the complete
production product row under `raw_legacy_values`. Missing research data remains
null or an empty array; the exporter does not guess values.

## Reference dictionaries

The package contains canonical IDs, names, aliases, active state and raw source
rows for manufacturers, formulations, components, crops, varieties, diseases,
pests and weeds. Units and application methods are exported as observed contract
codes. The import contract forbids automatic creation of canonical entities and
routes unknown IDs, new units, conflicts and ambiguous aliases to
`NEED_OWNER_REVIEW`.

Dictionary counts at export time:

- manufacturers: `14`;
- formulations: `6`;
- components: `432`;
- crops: `45`;
- varieties: `165`;
- diseases: `70`;
- pests: `31`;
- weeds: `41`;
- observed units: `5`;
- observed application methods: `4`.

## Validation

The source was read twice and independently rebuilt in memory. Both data
snapshots produced export fingerprint
`8f3b820a2542fbe8b4d634d4a3d51a7ff72f2dcebf226da1dc937ac08e910a0e`.
A separate complete exporter run also reproduced the exact same manifest hash:
`622f4ac9e6d8a47a7551bf32113eb3d445e21dbb2cbb9f827c9363f106cd99d9`.

The output directory is outside Git:
`C:/Users/TRAVKIN/Downloads/CodecSaaS/audit-output/TZ-202/`.

It contains:

- `pesticide_full_research_pack.json`;
- `pesticide_full_research_pack.csv`;
- `canonical_reference_dictionaries.json`;
- `existing_sources.json`;
- `import_contract.json`;
- `export_validation.json`;
- `manifest.sha256`.

No credential, email, access token or service key is present in the output.

## Safety result

- production write operations: `0`;
- QA write operations: `0`;
- production schema changes: `0`;
- company data exported: `0`;
- deploy: `NO`;
- master merge: `NO`.

## Decision

`FILES_READY_FOR_OWNER: YES`.

The owner can send the JSON or CSV research pack together with the canonical
dictionaries and import contract to an external researcher. Returned data must
still pass owner review, import preview, backup and live preflight before any
database apply.
