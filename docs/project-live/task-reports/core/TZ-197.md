# TZ-197 - Apply confirmed GLBD V1 corrections

STATUS: `PASS_APPLIED`

DATE: `2026-07-19`

BRANCH: `copilot-v1`

OWNER APPROVAL: `APPROVED`

## Scope

Applied only the nine source-backed actions frozen by TZ-196:

- `ES Biograno Forte`: `is_active true -> false`;
- `Amino Alexin`: `is_active true -> false`;
- `Lastik Extra`: category, subcategory, EC formulation and a two-component composition group;
- `Праймур, СЭ`: formulation correction and a two-component composition group.

The ten `BLOCKED_NO_DATA` cards and owner HOLD cards `Дитан`, `Метамил` and `Курзат` were excluded. Application rates, registration, target crops, company products and company business data were outside the transaction.

## Backup and preflight

- Fresh backup: `C:/Users/TRAVKIN/Downloads/CodecSaaS/audit-output/TZ-197/backups/glbd-v1-confirmed-corrections-20260718T225234587Z/`.
- Backup manifest SHA-256: `53c6ffb37b39264a7b145c7fc4f7217899c7faba3abfcd3a94eefc8da7429b89`.
- Frozen TZ-196 manifest SHA-256: `1d9f4a7e33888b7dbd1c2f708bec514c885ea729f9f32c12b385f20e953805af`.
- Package: `4` products / `9` actions; guessed values `0`; duplicate preview `0`; alias conflicts `0`.
- Baseline: products `1231`, components `432`, aliases `662`, sources `336`, component links `1389`, legacy links `1372`, company links `0`.

The backup contains full snapshots of products, aliases, formulations, components, component sources, V2 product-component links and legacy links, plus exact scoped rows, timestamps, fingerprints, apply SQL and rollback SQL.

## Transaction result

The first transaction attempt was rejected by `products_product_subcategory_check_v1` because the two rows being inactivated already contained legacy subcategories outside the current `NOT VALID` contract. PostgreSQL rolled the whole transaction back; products, sources and links remained exactly at the preflight state.

The final transaction temporarily removed and restored the exact same `NOT VALID` constraint inside one transaction. This allowed only the approved `is_active` fields to change without guessing or normalizing their legacy subcategories. The final constraint definition and validation state match the production baseline.

Final apply metrics:

- product field actions: `7/7`;
- component groups: `2/2`;
- total approved actions: `9/9`;
- supporting component sources: `4`;
- new component links: `4`;
- transaction: `COMMITTED`.

## Post-check

- Products remain `1231`; components remain `432`; aliases remain `662`; legacy links remain `1372`.
- Component sources: `336 -> 340`.
- V2 component links: `1389 -> 1393`.
- Company products remain `6`; company V2 links remain `0`.
- Non-approved fields on the four target products match backup exactly.
- All untouched catalog rows match backup exactly after excluding the four approved sources and four approved links.
- BLOCKED fingerprint: unchanged.
- HOLD fingerprint: unchanged.
- Company product fingerprint: unchanged.
- Active product duplicate groups: `0`.
- Active alias conflicts: `0`.
- Active component-link duplicates: `0`.
- Source duplicate groups introduced by TZ-197: `0`.
- Search: `Lastik Extra`, `Ластик Экстра` and `Праймур` all resolve to the expected active cards.
- No BLOCKED card acquired a component link.
- Second apply: product updates `0`, sources `0`, links `0`, approved actions `0`.
- Supabase production status: `ACTIVE_HEALTHY`.

## Assistant gate

TZ-196 preview-pending corrections are now applied. A110 may begin read-only GLBD work using the TZ-196 safety matrix together with this post-apply record. The ten `BLOCKED_NO_DATA` rows remain excluded, and agronomic recommendations remain outside the approved scope.

## Safety

- Production catalog data changed only in the approved global GLBD scope.
- Company/business data changed: `NO`.
- Schema final state changed: `NO`.
- Migration applied: `NO`.
- Deployment: `NO`.
- Master merge: `NO`.
- Rollback required: `NO`.
