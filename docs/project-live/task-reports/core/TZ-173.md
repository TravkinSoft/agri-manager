# TZ-173 - Apply safe blocked component sources

DATE: `2026-07-16`
STATUS: `STOPPED_AND_ROLLED_BACK`
CORE_BRANCH: `copilot-v1`
PRODUCTION_CHANGE: `ROLLED_BACK_TO_EXACT_BASELINE`

## Owner scope

The owner approved the exact TZ-172 selective package for 53 classified source
rows. `Humic acids / Гуминовые кислоты` remained `HOLD_OUT_OF_SCOPE`: no
component, alias, source, status or product-link action was generated for it.

## Backup and live preflight

A fresh external backup was created at
`C:/Users/TRAVKIN/Downloads/CodecSaaS/audit-output/TZ-173/backups/glbd-blocked-sources-20260716T182228754Z`.
Its SHA-256 manifest was verified before any write. The backup contains all 54
blocked rows, components, aliases, sources, product-component links, product
identity, status fields, schema fingerprint and exact rollback material.

Live preflight passed all fail-closed guards:

- blocked rows `54`, approved apply rows `53`, Humic HOLD rows `1`;
- direct attachments `3`, SAFE_ALIAS decisions `33`, safeners `6`, inactive
  decisions `10`, garbage rejection `1`;
- planned aliases `39` in total: 33 approved alias decisions plus six
  source-backed RU/EN safener aliases;
- alias, component, source and link conflicts `0`;
- production baseline `425` components, `24` aliases, `295` sources, `1373`
  product links and `0` company links;
- schema fingerprint `043a7c4719347b58a70b814d3b87beb8` over 116 objects;
- Humic fingerprint `14392c24608e1dbfc475e967e133ddab`, aliases `0`, sources `0`.

## Apply, smoke failure and rollback

The verified package was executed in one transaction. Database post-checks and
the second idempotence probe passed: approved classifications were processed,
all duplicate/conflict counters were zero, links stayed at `1373`, and the
second apply produced zero inserts, updates and deletes.

The required application-helper catalog smoke then failed. English safener
search worked, but Russian safener labels were stored as mojibake, for example
`РљР»РѕРєРІРёРЅС‚РѕС†РµС‚-РјРµРєСЃРёР»`, so RU alias/exact search failed. This is
an encoding defect in the approved TZ-172 package and is a release blocker.
No package repair was attempted inside TZ-173.

Failure handling was executed immediately. The exact rollback restored the
baseline. A production `updated_at` trigger initially changed timestamps on 41
components and 103 links during rollback; a guarded timestamp-only restore,
using the pre-write backup UUIDs and expected current timestamps, restored
those metadata values as well. Final byte-for-byte comparison passed for:

- `glbd_components`;
- `glbd_component_aliases`;
- `glbd_component_sources`;
- `glbd_product_components`;
- product identity rows.

Final production state is exactly `425/24/295/1373`, active components `415`,
company links `0`, unchanged Humic fingerprint and unchanged schema
fingerprint. Supabase is `ACTIVE_HEALTHY`. Seven critical production routes
returned HTTP 200 after rollback.

## Decision

`READY_FOR_NEXT_GLBD_STAGE=NO`. The safe next task is to regenerate only the
TZ-172 execution package with verified UTF-8 Russian safener labels, repeat the
isolated app-helper smoke, create a fresh backup and obtain a new explicit
owner approval before another production apply. Humic acids remains
`HOLD_OUT_OF_SCOPE`.
