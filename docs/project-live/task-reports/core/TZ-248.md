# TZ-248 - Seed & Planting Material Lifecycle V1

STATUS: `QA_PASS_PREVIEW_READY`
DATE: `2026-08-03`
BRANCH: `feat/seed-planting-material-lifecycle-v1`
BASE: `origin/master@fb7334cce039ffec7533f9f1b2fd2bbae51d8114`
QA_PROJECT: `gsglkmudcwkdetqtocae`
PREVIEW: `https://agri-manager-ptnzf0e5a-travkin-ais-projects.vercel.app`

## Result

- Ordinary sowing and planting now take the exact `crop_id + variety_id + seed_reproduction_id` identity from the selected crop-structure section.
- The agronomist sees one automatic primary seed/planting-material block and enters only the rate and its `kg/ha` or `t/ha` presentation unit.
- Canonical demand is stored in kilograms. The verified potato example is `12 ha x 3.2 t/ha = 38,400 kg`.
- A company-local technical product is created idempotently only where the existing operation/request/warehouse contracts require `product_id`. It is excluded from global and agrochemical catalogs.
- Receipts, batches, material requests, issue allocations, returns, reconciliation and field history preserve the same exact seed identity and source-batch trace.
- Different varieties or reproductions cannot satisfy the request. Multiple batches of one exact identity are supported.
- `crop_mix` keeps the TZ-242 multi-component lifecycle and is not forced into the ordinary single-identity rule.

## Schema

QA-only migrations applied in order:

1. `20260803125025_seed_planting_material_lifecycle_v1.sql`
2. `20260803131308_seed_planting_actual_rate_v1.sql`
3. `20260803134126_seed_planting_advisor_hardening_v1.sql`

The first migration adds the company seed identity and exact identity fields used by batches and request items. The next two preserve actual-rate history and add the task-owned FK indexes plus RLS planner hardening. Public and anon execution are revoked for the authenticated SECURITY DEFINER RPCs; actor, role, company and season checks remain mandatory.

## Acceptance

- Automated TZ-248 regression: `57/57 PASS`.
- Typecheck: `PASS`.
- Safe QA build: `PASS`, `145/145` pages, production ref matches `0`, QA ref matches `46`, service-role runtime `NO`.
- Potato lifecycle: two receipts (`20,000 + 30,000 kg`), one `38,400 kg` request, issue from two batches (`20,000 + 18,400 kg`), fact `38,000 consumed + 400 returned + 0 loss`, final exact stock `12,000 kg`.
- Wrong variety and wrong reproduction: `BLOCKED`.
- Same-identity multi-section: `PASS`; different-identity multi-section: `BLOCKED`; crop-mix regression: `PASS`.
- RLS/roles: Agronomist, Warehouse, Specialist, Company Admin and Global Admin passed; company B remained isolated.
- Closed-season create/write guard: `PASS`; rollback restored the test season.
- Repeat receipt, operation and issue: no duplicate rows or ledger movements.
- QA cleanup restored all eight audited business-table fingerprints; temporary seed identities remaining: `0`.
- Authenticated Preview UI: exact potato identity read-only, no seed-product selector/UUID, `3.2 t/ha -> 38.4 t`, deficit visible.
- Viewports: `1920x1080 PASS`, `1366x768 PASS`.

## Safety

- Production connections/writes/migrations/deploy at this checkpoint: `0 / 0 / NONE / NO`.
- Global seed products created: `0`.
- Existing IDs rewritten: `0`.
- Audit backups and screenshots remain outside Git in `audit-output/TZ-248/`.
- Production release remains gated by current-master/open-PR audit, release manifest, fresh production backup, migration apply, non-force PR merge, Vercel READY and authenticated `travkinflow.com` smoke.

## Commits

- `ec88722` - `feat(seed): add canonical seed material identity v1`
- `SELF` - `feat(operations): connect planting to warehouse seed batches`

## Next Action

Push the feature branch, open the PR to current `master`, complete the guarded production release sequence, and declare final PASS only after `travkinflow.com` smoke has `5xx = 0` and runtime errors `0`.
