# TZ-191 - Rebuild and apply 200 unique pesticide search aliases

DATE: `2026-07-18`
STATUS: `DONE`
CORE_BRANCH: `copilot-v1`
START_COMMIT: `abce1bb9e18fc118c68dfc6add6fb31d05ffe81c`
OWNER_APPROVAL: `APPROVED`
PRODUCTION_PROJECT: `bhsemlvmkikpntabctml`

## Rebuilt package

The owner rejected the ambiguous short alias `Идеал` for both separate cards:

- `Идеал, ВР`;
- `Идеал, КС`.

Both cards remain separate and unchanged. Their short aliases were replaced by
the first two exact collision-free Batch 06 candidates:

- `Листего Про, ВР` -> `Листего Про`;
- `Локустин, КС` -> `Локустин`.

The rebuilt package contains exactly `200` cards, alias IDs and normalized
aliases. Exact duplicate, owner and identity collisions are `0`. The verified
package manifest SHA-256 is
`238ea8dbe29c12469172e9f8dbaf6d9a3c512391a50f437d0c4d550032d35d0d`.

## Backup and live preflight

Fresh exact-scope backup:

`C:/Users/TRAVKIN/Downloads/CodecSaaS/audit-output/TZ-191/backups/pesticide-alias-batch-two-20260718T092501595Z`

- backup manifest SHA-256:
  `12b22709a0571cde861a11b74e1aab12c4b3179c480ead1d71319ca3abf5e1a0`;
- TZ-187 source manifest SHA-256:
  `7486e4bf0d272a4e3ea84673367a996e81f8b811500d7f9acb1be588f85579cf`;
- source batch-plan SHA-256:
  `b2851985a096422e5280476938155586b64b29a937da95a372df42b103f24c24`;
- cards found: `200/200`;
- aliases absent before apply: `200/200`;
- duplicate/conflict/foreign collision groups: `0/0/0`;
- exact projected search: `200/200`, ambiguities `0`;
- company targets: `0`;
- production health: `ACTIVE_HEALTHY`.

The verifier records `15` broader substring matches separately. They are
expected family-name results such as a short name and its `Forte/Super`
variant; every exact normalized alias has one owner and resolves to the target
card. This is distinct from the rejected `Идеал` collision, where two cards
would have owned the same exact alias.

## Production apply and acceptance

One guarded transaction inserted only the approved rows into
`global_product_aliases`.

- cards processed: `200`;
- aliases inserted: `200`;
- aliases total: `150 -> 350`;
- exact `Идеал` aliases after apply: `0`;
- replacement aliases present: `2/2`;
- duplicate groups: `0`;
- alias conflicts: `0`;
- exact search: `200/200 PASS`;
- exact search ambiguities: `0`;
- Curamin/Курамин/Фолиар and Phomazin/Фомазин controls: `5/5 PASS`;
- target product and company product fingerprints unchanged;
- second apply: `inserted=0`;
- critical route smoke: `6/6 HTTP 200`;
- final production health: `ACTIVE_HEALTHY`;
- rollback required: `NO`.

Trade names, manufacturers, formulations, components, registrations, usage
rules, products and company data were not changed. No migration, db push,
deploy or master merge was performed.

## Decision

`READY_FOR_NEXT_ALIAS_BATCH=YES_WITH_OWNER_APPROVAL`. The original TZ-187 plan
has `314` unapplied rows; the two permanently rejected `Идеал` aliases are not
eligible, leaving `312` owner-eligible candidates for future batches.
