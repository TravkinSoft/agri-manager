# TZ-243 - Global Fertilizers Catalog V1

DATE: `2026-08-02`

BRANCH: `copilot-v1`

TEST ENVIRONMENT: Supabase branch `gsglkmudcwkdetqtocae` and canonical Vercel QA Preview only

TZ No.243 STATUS: `PASS_WITH_MANUAL_SCREENSHOT_GAP`

## Source And Live Diff

- Accepted only `TravkinFlow_Fertilizers_Kazakhstan_Import_Master_V3.xlsx`:
  `95196 bytes`, SHA-256
  `302B81CAFF523E3E74499AC595E06E92D6A22C975EC93B07224BAA23861BEE04`.
- Strict XLSX validation passed: audit `419`, fertilizers `418`, additives
  `5`, categories `13`, application values `8`, brands `38`, unresolved
  categories `0`, normalized source duplicates `0`.
- Read-only live diff classified fertilizer rows as `416 ADD_NEW` and
  `2 UPDATE_CATEGORY`; all five EcoSave additives were `ADD_NEW`. Dangerous
  or ambiguous merge decisions: `0`.
- Existing Curamin Foliar and Phomazin IDs were preserved. Identity is based
  on manufacturer plus normalized trade name; composition is never a merge
  key.

## QA Apply

- One canonical migration
  `20260802100000_global_fertilizers_catalog_v1.sql` was applied only to QA.
- QA now contains `419` active global fertilizers: all `418` source identities
  plus one unrelated pre-existing fertilizer, and exactly `5` imported
  additives.
- `fertilizer_categories` contains the exact `13` functional categories.
  Application is stored separately in `application_scope`.
- BIoLIP is an `additive`; SILTEK, PH POWER, PEN-OFF and Hard-OFF keep their
  exact Russian source categories. Aminosid POWER remains a fertilizer.
- Source rows missing, checksum mismatches, duplicate identities and category
  gaps are all `0`.
- Repeating the canonical SQL twice after the first apply made no product-row
  or timestamp changes. Source fingerprint remained
  `34a8ebdd2c6451e0de84edd2e9879c3c`.

## UI And Material Contracts

- Existing Fertilizers page now exposes search plus category, manufacturer and
  application filters and the required name, manufacturer, category,
  composition, form and unit columns.
- Existing Pesticides page accepts pesticide, additive, adjuvant and growth
  regulator types and excludes fertilizer.
- Additives and Growth Regulators were removed from the sidebar. Their legacy
  routes redirect to the filtered Pesticides list and do not create parallel
  catalog pages.
- Operation, warehouse and Crop Care product contracts retain company override
  dedupe. Fertilizers remain available where fertilizer is allowed, additives
  remain available to spraying/tank mixes, and water remains excluded as a
  material.
- QA currently has `0` source rows with `product_type=growth_regulator`.
  Rendering/filtering and the legacy redirect are covered by the code contract,
  but a live growth-regulator card could not be inspected without inventing
  data.

## Security And Verification

- Global Admin can maintain global products and global fertilizer categories.
  Company Admin, Agronomist, Warehouse and Specialist can read all `423`
  source products and `13` categories but cannot mutate or delete them.
- Cross-company leakage is `0`; every write probe ran inside a rolled-back
  transaction.
- Supabase Advisors report no task-specific security finding. Two new
  `unused_index` notices are expected before wider catalog-filter traffic.
- Typecheck: `PASS`.
- QA public-env production build: `PASS`.
- Automated checks: `53/53 PASS`.
- Authenticated data/API, search, dedupe, RLS and idempotency smoke: `PASS`.
- Browser screenshot regression: `NOT COMPLETED`; Chrome control detached twice
  after QA sign-in. No visual PASS is claimed for that missing evidence.

## Safety

- Pre-apply backup, exact rollback SQL, live diffs, automated/RLS/idempotency
  evidence and SHA-256 manifest are outside Git under `audit-output/TZ-243/`.
- QA business counts stayed `17 fields / 74 operations / 7 warehouses / 72
  ledger rows`.
- Production remained read-only and unchanged: products `1231`, fertilizers
  `306`, additives `47`, fields `100`, operations `8`, warehouses `2`, ledger
  `12`. Production writes/migrations/deploy: `0/0/NO`; master unchanged.

## Decision

- `P0_REMAINING=0`.
- `P1_REMAINING=0`.
- `P2_REMAINING=1` - repeat visual screenshots when browser control is stable;
  QA also has no live growth-regulator fixture.
- `READY_FOR_FATHER_CONTROLLED_QA_PILOT=YES`.
- `READY_FOR_PRODUCTION=NO`.
