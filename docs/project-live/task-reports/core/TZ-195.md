# TZ-195 - Integrate A109 into Core preview

DATE: `2026-07-19`
STATUS: `DONE_LOCAL_AND_REAL_QA_PREVIEW_PENDING`
CORE_BRANCH: `copilot-v1`
START_COMMIT: `26a9813d787d8c9ac00b7a80fe3a24286daa5677`
ASSISTANT_SOURCE_COMMIT: `f503f05b54de129b6778fee10e3b6406ec761ced`
TEST_BRANCH_REF: `gsglkmudcwkdetqtocae`

## Integration scope

A108 and A109 were compared directly. A109 is one commit on top of A108. Core
received only the verified read-only runtime changes, the new context-scope
helper and the A109 mocked/REAL QA harnesses. No merge or rebase was used.

Assistant Live documents, audit-output, `.gitignore`, env files, secrets, local
runtime launchers and unrelated Assistant changes were not transferred.

The integration provides:

- company-wide field and operation scope after a focused field question;
- explicit Field 15 parsing and strict planned/in-progress/completed wording;
- deterministic all-field handling for `Какие это поля?`;
- grounded recovery after an empty DATA response;
- conservative Curamin typo/alias matching;
- one-tool deduplication and independent field-search routing required to keep
  the A107 contract deterministic in Core.

## Acceptance

- typecheck: `PASS`;
- clean branch-only production build: `PASS`;
- production Supabase URL/value matches in bundle: `0`;
- real service-role value matches in bundle/runtime: `0`;
- A109 mocked scope regression: `10/10 PASS`;
- A107 REAL ERP regression: `45/45 PASS`, numeric accuracy `100%`;
- A109 REAL regression: `12/12 PASS`;
- A109 critical scope chain: `4/4 PASS`;
- greeting/memory policy regression: `4/4 PASS`;
- Core authenticated functional smoke: `PASS`;
- QA data: `9` crop rows, `2` warehouses, `5` operations;
- balances: nitrate `1550 kg`, Curamin Foliar `520 l`, Phomazin `200 l`;
- cross-company leaks: `0`;
- ERP writes: `0`;
- production connections: `0`;
- service role loaded: `NO`.

The test runtime used only `gsglkmudcwkdetqtocae`, real QA User A JWT and
RLS. Production Supabase, production deployment and ERP mutations were not
used.

## Preview gate

`origin/copilot-v1` push must create a non-production Vercel Preview. Its final
URL and READY status are reported by the TZ-195 execution result after deploy
verification. Master merge and production deploy remain forbidden.
