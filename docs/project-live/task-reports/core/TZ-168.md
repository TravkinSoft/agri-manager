# TZ-168 - Production catalog security hardening apply

DATE: `2026-07-16`
STATUS: `BLOCKED_SAFE_STOP_NO_PRODUCTION_CHANGE`
CORE_BRANCH: `copilot-v1`
PRODUCTION_PROJECT: `bhsemlvmkikpntabctml`

## Result

The owner-approved production apply was stopped safely at the first DDL
statement. The exact candidate
`20260716114950_catalog_rls_function_security_hardening.sql` was not applied.
PostgreSQL returned SQLSTATE `3F000`: `schema "private" does not exist` while
creating `private.is_active_global_admin()`.

This is a hidden dependency on the branch-only Assistant Memory migration. The
isolated TZ-167 branch had schema `private`, but production intentionally does
not contain that non-production Assistant scope even though canonical migration
metadata includes version `20260714140722`. The approved security migration
therefore cannot be replayed unchanged against the production schema.

## Safety evidence

- candidate commit `8fdc5ac7686106e79211b064c1e1ad804aaaa341` is in `origin/copilot-v1`;
- candidate SHA-256 is
  `d958974c41ff41a78139d3ccf52793d27ad4ae7a0b22e54e55113d579f79d3dc`;
- fresh backup is outside Git at `audit-output/TZ-168/pre-apply-backup/`;
- verified backup manifest SHA-256 is
  `040b2bd15c00367b899e3d28787fde3aa324bfb965c99f7898b9208cb4305251`;
- preflight matched the canonical production schema fingerprint
  `e60ac8740c8a76adad8a1b792a2314827047305559e0b156cfedd0d23fdd5a9d`;
- history remained `135`, head `20260714140722`, and target version was absent;
- exact target counts remained `9, 5, 97, 55, 28, 11, 3, 155`;
- after the failed apply, all eight target tables still had RLS disabled,
  policies remained zero, and all 27 original definer functions remained
  unchanged;
- a separate rolled-back diagnostic transaction reproduced the same server
  error as user `postgres`;
- production and `assistant-memory-a106` both remained `ACTIVE_HEALTHY`;
- `/auth/login`, `/dashboard`, `/references`, `/operations`,
  `/crop-structure` and `/warehouses/requests` returned HTTP `200`.

No business data, Auth data, schema object, migration-history row, application
deployment, master branch or Assistant branch was changed. No manual rollback
was required because the migration transaction failed atomically.

## Required next gate

Open a separate owner-approved task to remove the hidden branch-only dependency.
The revised candidate must create and lock a dedicated non-exposed helper schema
with exact schema `USAGE` grants, or use another reviewed production-safe
placement. It must then be tested against a production-equivalent baseline that
does not contain Assistant Memory V1, receive a new SHA/commit, repeat the full
TZ-167 JWT matrix, and obtain fresh production approval. The current migration
must not be retried unchanged.
