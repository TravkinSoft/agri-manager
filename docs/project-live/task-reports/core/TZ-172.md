# TZ-172 - Classify all 54 blocked GLBD component sources

DATE: `2026-07-16`
STATUS: `PASS_WITH_OWNER_HOLD`
CORE_BRANCH: `copilot-v1`
PRODUCTION_CHANGE: `NONE`

## Baseline and boundaries

Production project `bhsemlvmkikpntabctml` was checked read-only and remained
`ACTIVE_HEALTHY`. Catalog security hardening is present. The verified baseline
is 425 components, 1373 global product-component links, 0 company links, 295
component sources, 24 aliases and 136 migration-history versions with head
`20260716114950`. No production SQL, migration, deploy, merge or business-data
write was performed.

The complete audit and apply package are outside Git at
`C:/Users/TRAVKIN/Downloads/CodecSaaS/audit-output/TZ-172/`.

## Classification result

All 54 blocked rows were classified exactly once:

| Decision | Rows |
| --- | ---: |
| `SAFE_ATTACH_EXISTING` | 3 |
| `SAFE_ALIAS` | 33 |
| `SAFE_SAFENER` | 6 |
| `SAFE_BIOLOGICAL` | 0 |
| `KEEP_INACTIVE` | 10 |
| `GARBAGE_REJECT` | 1 |
| `NEED_OWNER_REVIEW` | 1 |

The three direct attachments are Ethephon, Citric acid and Succinic acid. The
33 aliases contain proven active-ingredient synonyms and legacy safener
spellings. Six distinct safener identities were confirmed: Cloquintocet-mexyl,
Cloquintocet free acid, Mefenpyr-diethyl, Naphthalic anhydride,
Cyprosulfamide and Fenchlorazole-ethyl. Ten already archived RU/EN duplicate
rows remain inactive. `Бентазон 25%` is rejected as a concentration fragment
after an exact planned relink to Bentazon.

`Humic acids / Гуминовые кислоты` remains `NEED_OWNER_REVIEW`: the available
evidence describes a broad heterogeneous class rather than one unambiguous
component identity. The apply preview performs no action for this row.

No blocked biological source row was found. A separate review inventories all
22 live biological components and preserves species/strain distinctions.

## Safener identity correction

The safener review found one existing visual duplicate outside the 54 source
rows: `клоквинтоцет-мексил` was typed as an active ingredient and had six
legacy active-role links. The selective preview includes a fail-closed related
fix: those exact six link UUIDs move to the canonical safener with role
`safener`, then the mislabeled legacy component is archived. This avoids
creating a second visible Cloquintocet-mexyl identity and keeps the total link
count at 1373. Four generic, combined or concentration-in-name candidates stay
outside the selective package and are documented for future review.

## Apply package and isolated test

The guarded package contains exact UUID lists, no broad product update, no
company-data statement and an exact rollback. Its planned selective effect is:

- create 6 canonical safener components;
- add 23 verified sources and 39 aliases;
- normalize 103 exact product-component links, including 6 role corrections;
- archive 41 proven duplicate/parser rows without physical deletion;
- preserve all 1373 product-component links and 0 company links.

The package was applied only to an isolated PGlite/PostgreSQL-compatible copy
of the read-only production snapshot. First apply passed; second apply was a
true no-op; exact rollback restored the original fingerprint. Post-checks:

- active component duplicates: 0;
- alias conflicts: 0;
- product-component duplicate links: 0;
- source duplicate links: 0;
- manufacturer names created as components: 0;
- garbage components created: 0;
- RU/EN exact search for Cloquintocet-mexyl and Mefenpyr-diethyl: PASS;
- counts: `425/1373/24/295 -> 431/1373/63/318 -> 425/1373/24/295`.

The SHA-256 manifest verifies every package file. A future production apply
must exclude the Humic-acids HOLD, create a fresh backup, repeat live preflight,
verify the manifest and receive separate explicit owner approval.

READY_FOR_SELECTIVE_APPLY: `YES_WITH_OWNER_APPROVAL_AND_HUMIC_HOLD`
