/**
 * TravkinFlow 2 functional rollout is complete. Keep one code-level switch so
 * the whole functional package can be disabled by a rollback deployment.
 */
export const TRAVKINFLOW_2_FUNCTIONS_RELEASED = true;
